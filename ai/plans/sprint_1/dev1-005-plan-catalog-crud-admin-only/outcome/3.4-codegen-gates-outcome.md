# Task 3.4 — Codegen, SDL Verification & No-Delete-Surface Assertion — Outcome

Plan: `ai/plans/sprint_1/dev1-005-plan-catalog-crud-admin-only/` · Phase: phase-3-graphql-resolvers-api · Task: 3.4
Base HEAD: `c70248b` (expected) · Deliverable branch: `origin/feat/dev1-005-plan-catalog-crud-admin-only`

## Scripts used (verified against package.json)

Tasks.md's shorthand `bun run generate:gqlSchema && bun codegen` matches the real script keys exactly:

| Script | Underlying command | Output |
| --- | --- | --- |
| `generate:gqlSchema` | `bun run scripts/generate-gql-schema.ts` | `frontend/graphql/generated/schema.graphql` (Pothos `graphQLSchema` → `printSchema(lexicographicSortSchema(...))`, deterministic) |
| `codegen` | `graphql-codegen --config codegen.ts` | `frontend/graphql/generated/gql/graphql.ts` (typescript-operations + typed-document-node over `frontend/graphql/sharedDocuments/**` + `frontend/views/**/*.documents.ts`) |

Both ran clean. `generate:gqlSchema` rewrote the committed SDL (+37 lines — the full billing surface).
`codegen` ran green and produced **NO diff** in `frontend/graphql/generated/gql/graphql.ts`: the typed-document
artifact is generated from the operation documents, and no sharedDocument/view document references the billing
surface yet, so the previously committed `graphql.ts` remains byte-identical and in lockstep (REQ-077). When
frontend documents for the catalog land (later task), re-running `bun codegen` will extend it.

## Generated artifacts committed (REQ-060/077)

- `frontend/graphql/generated/schema.graphql` — regenerated; contains the complete billing surface (see REQ-060 below).
- `frontend/graphql/generated/gql/graphql.ts` — unchanged by this task (no billing documents exist yet); verified present/tracked, no untracked leftovers under `frontend/graphql/generated` (step 7 check: clean).

## INV-PC3 — no-delete surface assertion (REQ-020), PERSISTENT test

New file `backend/graphql/test/billing/schema-gates.test.ts` (self-contained unit tier: schema printing + file
asserts only — NO server boot, NO network, NO DB; runs via the mandated runner
`bun run test/scripts/run-test.ts backend/graphql/test/billing/schema-gates.test.ts`). Four layers:

1. **Live schema** — `printSchema(lexicographicSortSchema(graphQLSchema))` (same self-contained emission pattern as
   `schema-surface.test.ts`, so the test does NOT depend on codegen having run in CI) must not match
   `/deleteplan|removeplan/i` anywhere.
2. **Committed artifact** — `frontend/graphql/generated/schema.graphql` (tracked in git ⇒ present in CI checkouts)
   must not match `/deleteplan|removeplan/i` — catches manual/hand edits of the generated SDL.
3. **Root-field name walk** — no Query/Mutation root field name matches `/delete|remove/i`.
4. **Positive lifecycle pin** — `setPlanActiveStatus` IS present as the only lifecycle mutation; `deletePlan`/`removePlan`
   are absent from the mutation field set.

## REQ-060 — byte-for-byte SDL shape assertions

Two tiers in `schema-gates.test.ts`:

- **Committed-SDL tier (byte-for-byte)** — exact template-literal blocks asserted against the file:
  `type Plan { … }` (all ten fields in lexicographic emission order with exact types/nullability, `deactivatedAt: DateTime`
  the single nullable field), `input CreatePlanInput { … }` (five REQUIRED fields incl. `price: String!`), `input
  UpdatePlanInput { … }` (five OPTIONAL fields), the exact root-field lines `adminPlans(includeInactive: Boolean = true):
  [Plan!]!`, `planCatalog: [Plan!]!`, `createPlan(input: CreatePlanInput!): Plan!`, `updatePlan(id: ID!, input:
  UpdatePlanInput!): Plan!`, `setPlanActiveStatus(id: ID!, isActive: Boolean!): Plan!`, and `scalar DateTime`.
  BOPLA negative probes: `isActive`/`deactivatedAt` must not appear inside either input block.
- **Live-schema tier (codegen-independent cross-check)** — `Plan` discloses exactly the ten contract fields with exact
  type strings; both inputs expose exactly the five wire fields (Create all `!`-required, Update all nullable); root
  field signatures + `includeInactive` default `true`; `DateTime` is a `GraphQLScalarType`. (Task 3.1's
  `plan-schema-surface.test.ts` continues to lock the Plan block + scalar behavior; the two suites are complementary —
  8/8 still green.)

## SEC — root-field scope audit

The `authScopes` blocks are RUNTIME config (Pothos plugin), not SDL — so the persistent assertion reads them from the
built schema's field extensions (`field.extensions.pothosOptions.authScopes`), mirroring the established lint-clean
pattern in `frontend/graphql/test/gateway/allowlist-coverage.test.ts` (`isRecord` + `Reflect.get`; no type assertions).
Encoded as tests (all pass):

- `createPlan` / `updatePlan` / `setPlanActiveStatus` ≡ `{$all: {authenticated: true, role: [UserRole.Admin]}}` (admin gate, 401/403 split).
- `planCatalog` ≡ `{$all: {authenticated: true}}`; `adminPlans` ≡ `{$all: {authenticated: true, role: [UserRole.Admin]}}`.
- Pre-existing mutations `login` / `logout` / `refreshToken` / `registerUser` keep their pre-DEV1-005 scopeless posture (undefined) — unchanged.
- Mutation root set is EXACTLY baseline + the three sanctioned plan mutations.

**SDL-audit enumeration (complete, current generated SDL):**
`Query`: `_health` (scopeless, allowlisted probe), `adminPlans` (admin, new), `me` (`{authenticated:true}`, pre-existing),
`myApplicantProfile` (`$all{authenticated, role:[teacher]}`, DEV2-004), `planCatalog` (authenticated-any-role, new),
`recitationReadings` (scopeless public, pre-existing). `Mutation`: `createPlan` / `updatePlan` / `setPlanActiveStatus`
(admin, new), `login` / `logout` / `refreshToken` / `registerUser` (scopeless-by-source, pre-existing — session/public
ops gated per their own design and allowlist coverage). **No ungated NEW surface beyond what DEV1-005 authorized.**
Behavioral proof of the 401/403 split for all five new fields already exists in the Task 3.2/3.3 integration suites
(7/7 + 17/17 over the wire).

## schema-surface freeze-list reconciliation

`backend/graphql/test/schema-surface.test.ts` was failing 4/12 after the billing surface joined (3 pre-existing on the
base commit — DEV2-004 drift, proven unrelated to billing by Task 3.1 — plus the mutations freeze broken by our trio):

- Mechanism: closed `PRE_3_1_*` tuples (baseline captured @ `8e5ebb8`) + "exactly one addition" expectations; no
  snapshot-hash re-record env-var exists — reconciliation is done in-code.
- Minimal, mechanism-preserving edits: the baseline tuples stay untouched (historical record); a new
  `POST_BASELINE_*_ADDITIONS` section lists the CLOSED, reviewed post-baseline additions —
  queries `["_health", "adminPlans", "myApplicantProfile", "planCatalog"]`, mutations
  `["createPlan", "setPlanActiveStatus", "updatePlan"]`, enums `["ApplicantStatus"]`, types
  `["ApplicantProfile", "ApplicantStatus", "CreatePlanInput", "DateTime", "HealthCheck", "Plan", "UpdatePlanInput"]` —
  merged with a small `frozenSet()` helper (same `localeCompare` ordering as the walk). Headers/test titles updated to
  say "baseline + sanctioned additions"; stale "must remain UNCHANGED forever" comment corrected.
- Result: **12/12 pass** (was 8/4).

## Gate results

| Gate | Result |
| --- | --- |
| `bun run generate:gqlSchema` | OK (+37 lines SDL) |
| `bun run codegen` | OK, no diff in `gql/graphql.ts` (no billing documents yet) |
| `bun run test/scripts/run-test.ts backend/graphql/test/schema-surface.test.ts` | 12 pass / 0 fail (41 expects) |
| `bun run test/scripts/run-test.ts backend/graphql/test/billing/schema-gates.test.ts` | 18 pass / 0 fail (77 expects) |
| `bun run test/scripts/run-test.ts backend/graphql/test/billing/plan-schema-surface.test.ts` | 8 pass / 0 fail (26 expects) — no regression to 3.1 |
| `bun run tsgo` | 0 errors in ALL files touched by 3.4. Repo-wide run shows exactly ONE error in `backend/db/test/logic/billing/plan-catalog-seed.test.ts` — that is the PARALLEL Task 3.5 agent's in-progress file (`backend/db/**` is exclusively theirs); out of scope here and expected to be fixed by 3.5 before its own commit. |
| `bun run scripts/health/sub-loop.ts backend/graphql/test/schema-surface.test.ts backend/graphql/test/billing/schema-gates.test.ts --lifecycle duplicates` | exit 0 — tsgo/oxlint/biome/lint:type-aware/check:duplicates all green on both hand-authored files (generated artifacts excluded per task — verified via tsgo only). |

## Lint-config note (no config modified)

The repo's lint tooling ALREADY excludes the generated dir — verified, nothing changed:
`biome.json` ignores `!frontend/graphql/generated`; `eslint.config.mjs` lists `frontend/graphql/generated/**` twice;
`.jscpd.json` excludes `frontend/graphql/generated/**`; tsconfig excludes `frontend/graphql/generated` from tsgo.
One oxlint finding surfaced during the sub-loop (`no-unsafe-type-assertion` on the extension reader) and was fixed in
the test itself by mirroring the `allowlist-coverage.test.ts` `isRecord`/`Reflect.get` pattern — no suppressions, no
config edits.

## Deviations / notes

- **Script keys**: as-specified (`generate:gqlSchema`, `codegen`) — confirmed against package.json, no rename needed.
- **Test location**: `backend/graphql/test/billing/schema-gates.test.ts` (sibling of 3.1's `plan-schema-surface.test.ts`,
  same unit-tier runner as `schema-surface.test.ts`).
- **SEC approach**: stronger than the "document-only" fallback offered by the task — the authScopes audit is a
  persistent test reading runtime extensions, since the mechanism proved fully introspectable. The integration 401/403
  proof remains with the 3.2/3.3 suites.
- **Freeze reconciliation** is in-code (the test has no snapshot-hash/re-record env-var mechanism; the `@8e5ebb8`
  reference is a comment-level provenance note, now supplemented with the `c70248b` reconciliation note).
- **Out-of-scope finding**: `bun codegen` shows no pre-existing failures; the only red gate repo-wide is 3.5's
  in-progress seed test (see gate table).
- 3.5's working-tree files (`backend/db/seeds/**`, `backend/db/test/logic/billing/plan-catalog-seed.test.ts`) were
  never staged; `worklog.md`, `db/`, `skills/` remain uncommitted per sandbox rules.
