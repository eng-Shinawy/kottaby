# Task 7.2 — Invariant & Decision Addenda — Outcome

> Plan: `ai/plans/sprint_1/dev1-005-plan-catalog-crud-admin-only/` · Task: 7.2 (docs-only)
> Base HEAD: `4297323` (`docs(billing): canonical plan-catalog reference doc (REQ-080)` — task 7.1)
> Scope guard honored: NO code files, NO AGENTS.md files, NO build/dev-server actions.

## Deliverables

### 1. `docs/specs/state-machine-invariants.md` — new §11 "Plan Catalog Lifecycle"

Appended after §10 (Evaluation Lifecycle), mirroring the file's section format (States table / mermaid transitions / Invariants table):

- **§11.1 States** — Active (`is_active = true`, `deactivated_at IS NULL`) vs Deactivated/Retired (`is_active = false`, `deactivated_at = <ts>`), with catalog/purchasability semantics per state.
- **§11.2 Allowed Transitions** — mermaid `stateDiagram-v2`: `[*] → Active` (createPlan / seed bootstrap via `insertPlan`, server-owned defaults), `Active → Deactivated` (`setPlanActiveStatus(id, false)` stamps `deactivated_at`), `Deactivated → Active` (reactivation **clears** `deactivated_at`). Prose note: state moves only through the guarded primitive `PlanRepository.setActiveStatusOnce` (TOCTOU window = 0); `updatePlan` never transitions state; no terminal state — rows are retired, never deleted.
- **§11.3 Invariants** (IDs `INV-PC1..PC3` — no collision with INV-S/TV/A/B/W/U/P/PAY/HW/PR/E families):
  - **INV-PC1** — deactivated plan never appears in the active catalog (`planCatalog`) and is never purchasable while inactive; `PlanRepository.listActive` is the single source of the `is_active` predicate (both consumer reads route through it); purchase-time re-validation deferred to DEV1-006 (D2 forward contract).
  - **INV-PC2** — deactivation/edit never mutates existing subscriptions or credited balances; byte-identical preservation (forward-only INV-B2/B3 shield), proof = outcome [5.1](5.1-preservation-proof-outcome.md); forward-only edit semantics (REQ-018).
  - **INV-PC3** — no hard deletion of plan rows anywhere in code or SDL; enforced by the schema-gates test (`deletePlan`/`removePlan` absent from printed SDL and committed `schema.graphql`).
- Header block links the canonical reference [`docs/billing/plan-catalog.md`](../../../docs/billing/plan-catalog.md) and cites the guarding tests: `plan-catalog-schema.test.ts` (lifecycle-pair invariant — "every persisted row satisfies the lifecycle-pair invariant: is_active = (deactivated_at IS NULL)"), `backend/graphql/test/billing/schema-gates.test.ts` (INV-PC3 gate), `plan-catalog.roles.test.ts` (FULL role matrix / visibility split), `plan-catalog-preservation.test.ts` (preservation proof).

### 2. `docs/specs/open-decisions-and-gaps.md` — new section "D. Plan Catalog Lifecycle Addendum (DEV1-005)"

Numbering continues the register **A (10) / B (18) / C (5) → D (7)** — zero collision with the original 33 decisions. Each entry follows the register format (`### D.N: Title` + `> **✅ RESOLVED**` blockquote with **Decision:** / **Schema impact:** or **Spec impact:**):

| ID | Decision | Key points |
|---|---|---|
| D.1 | Activation-flag schema delta | `plans.is_active` (NOT NULL DEFAULT true) + `plans.deactivated_at` (nullable), push-only; lifecycle-pair invariant `is_active = (deactivated_at IS NULL)` (A-category gap per plan.md D1) |
| D.2 | Reactivation semantics & durable audit history | `deactivated_at` CLEARED on reactivation; no per-row history in MVP; durable audit history owned by DEV3-020 (D1 deferral via `emitPlanAuditSeam`) |
| D.3 | Forward-only edits, no price snapshot | REQ-018; **revisit trigger:** first renewal / auto-billing / historical-invoicing flow must add a snapshot surface first |
| D.4 | Title-encoded taxonomy reaffirmed | FR-2.2 / REQ-019; **NO `plan_type`/`kind` column** — adding one is schema drift |
| D.5 | Verification-plan lookup rule ownership | FR-2.3 → consumed by DEV1-006/DEV2-005; stable title `"New Teacher Verification & Evaluation Plan"` + `sessionCount = 5`; canonical constant `VERIFICATION_PLAN_TITLE`; seeded via `PlanCatalogService` bootstrap only |
| D.6 | Create double-submit tolerance | REQ-043: title deliberately NOT unique — duplicate creates yield distinct rows; no 23505 path; cleanup = deactivate, never delete |
| D.7 | No-pagination / no-index / no-search rulings | REQ-034/REQ-046-area; **revisit triggers:** catalog size past dozens-scale threshold (pagination/index) and first search feature (`escapeLikeWildcards` mandatory) |

Consistency updates in the same file:
- Header **Purpose** line now notes the addendum (33 original + 7 = 40 total).
- **Summary** table: new row "Plan Catalog Lifecycle Addendum (DEV1-005) | 7 | ✅ All Resolved"; total updated 33 → 40.
- **Schema Changes Summary** table: new row `plans.is_active`, `plans.deactivated_at` added | D.1 | lifecycle flag + retirement marker (push-only, pair invariant).

## Gates

| Gate | Result |
|---|---|
| Numbering consistency | ✅ D.1–D.7 unique; grep-verified no `D.` collision with A.1–A.10 / B.1–B.18 / C.1–C.5; INV-PC1..PC3 unique vs all existing INV-* families |
| Doc-lint | ℹ️ NO docs-lint tool exists in the repo (no `lint:docs`/`markdownlint` in `package.json`) → absence noted, nothing to run (same as task 7.1) |
| Mermaid | ✅ `bun run validate:mermaid` — all diagrams valid (new §11.2 block included) |
| Cross-linked files | ✅ all referenced paths exist (docs/billing/plan-catalog.md, 4 guarding test files, outcome 5.1/5.2, seed-plan-catalog.ts, relative links from docs/specs/) — script-checked |
| `bun run tsgo` | ✅ 0 errors (doc-only change; type-check unaffected) |
| Scope | ✅ zero code/AGENTS.md files touched; no build, no dev-server restart |

## Files Changed

- `docs/specs/state-machine-invariants.md` (edited — §11 appended)
- `docs/specs/open-decisions-and-gaps.md` (edited — section D + summary/total/schema-table consistency)
- `ai/plans/sprint_1/dev1-005-plan-catalog-crud-admin-only/outcome/7.2-spec-addenda-outcome.md` (new — this file)
- `worklog.md` (appended Task ID: 7.2)

## Stage Summary

- Both spec addenda landed exactly as the 7.1 canonical doc anticipated (its "Related Documents" pointers to INV-PC1..PC3 §11 and the decision addenda now resolve to real sections — no dangling references left).
- Deferred items unchanged: D1 → DEV3-020 (audit), D2 → DEV1-006 (purchase-time re-validation contract recorded in INV-PC1).
- Doc-only change: 2 spec edits + this outcome + worklog appendix; tsgo 0; all cross-links verified.
