# Task 6.1 — Review Wave: Canonical Types & GraphQL Artifacts (REQ-003 / REQ-042 / REQ-060)

> HEAD at review start: `5f0c78e` (expected anchor confirmed). Postgres UP (:5432), dev server untouched (:3000).
> **Review-only wave: ZERO fixes required.** Every check passed on the committed state; no production or artifact file was modified.

## Verdict per check

| # | Check | Verdict |
|---|---|---|
| 1 | `backend/types/billing/plan.types.ts` — canonical naming + BOPLA-construction + import hygiene | **PASS** |
| 2 | Pothos files — no local type definitions; canonical-backed | **PASS** |
| 3 | Codegen artifacts — committed, zero drift, no manual edits, excluded from tsgo/lint | **PASS** |
| 4 | i18n type layer (`shared/locale/types/plans`) + parity tests coherent at 47 keys | **PASS** |

## Check 1 — Canonical types (`backend/types/billing/plan.types.ts`) — PASS

- **Canonical naming**: `PlanSelectType = typeof plans.$inferSelect`, `PlanInsertType = typeof plans.$inferInsert`, `PlanReturnType`, `PlanSubmitInput`, `PlanUpdateInput` — re-exported through `backend/types/billing/index.ts` → `backend/types/index.ts` (`export * from "./billing"`). Canonical home only.
- **BOPLA-construction**:
  - `PlanReturnType = Readonly<PlanSelectType>` — a *wrapper* (mapped type), not an alias, so any future field exclusion made on the select type keeps flowing into the return type. The comment block documents exactly this intent.
  - `PlanSubmitInput`: 5 readonly commercial fields (`title`, `sessionCount`, `price: string`, `currency`, `intervalDays`); `id`/`isActive`/`deactivatedAt`/`createdAt`/`updatedAt` are **structurally absent** (not `?:`-optional — absent). `price` is a decimal string.
  - `PlanUpdateInput`: mapped `{ readonly [K in keyof PlanSubmitInput]?: PlanSubmitInput[K] }` — partial, read-only, cannot gain server-controlled fields.
  - Negative proof: `plan.types.test-d.ts` carries 8 `@ts-expect-error` assertions (isActive/deactivatedAt/id/createdAt/updatedAt forbidden on submit; lifecycle field forbidden on update; `price: number` rejected on both payloads) — all still erroring as intended (tsgo exit 0 means every `@ts-expect-error` is *consumed*; an unconsumed one would fail the build).
- **No service-local `.types.ts`**: `find backend/services backend/graphql backend/db/repo -name "*.types.ts"` → **empty**. Sole `.types.ts` for plan is `backend/types/billing/plan.types.ts` (glob `backend/**/*plan*.types.ts` → 1 hit; `backend/**/**.service.types.ts` → 0 hits).
- **`DBTransaction` import hygiene** (grep over repository/service/resolvers/tests):
  - `backend/db/repo/billing/plan.repository.ts:29` — `from "@/backend/types"` ✓
  - `backend/services/billing/plan-catalog.service.ts:49` — `from "@/backend/types"` (inside a combined type-import with PlanInsertType/PlanReturnType/PlanSelectType/PlanSubmitInput/PlanUpdateInput/ApiFieldErrorType) ✓
  - `backend/db/test/logic/billing/plan-catalog-schema.test.ts:38`, `plan-catalog-preservation.test.ts:50` — `from "@/backend/types"` ✓; `backend/db/test/test-utils.ts:13` re-exports from `@/backend/types` ✓; seed `backend/db/seeds/billing/seed-plan-catalog.ts:4` — `from "@/backend/types"` ✓
  - Resolvers (`backend/graphql/mutation/plan-catalog.mutation.ts`, `backend/graphql/query/billing/plan-catalog.query.ts`) import **no** `DBTransaction` — correct: thin resolvers delegate to the service, which owns the optional-tx parameter. Not a defect.
  - Zero hits for a drizzle-internal transaction type (`PgTransaction`/`NodePgDatabase`-sourced) anywhere in the plan's files.

## Check 2 — Pothos files — PASS

- `backend/graphql/pothos/billing/plan.pothos.ts`: `gqlSchemaBuilder.objectRef<PlanReturnType>("Plan")` — backed **exclusively** by the canonical `PlanReturnType` imported from `@/backend/types` (line 40). All ten fields are structural exposures (`exposeID/exposeString/exposeInt/exposeBoolean`) or DateTime-scalar passthroughs; zero resolver-local type definitions, zero field-level business logic. `id` rides `ID!` (Apollo cache normalization), `price` rides `String!` (no Float).
- `backend/graphql/pothos/billing/plan-inputs.pothos.ts`: two `inputType` refs built from builder primitives only. SDL-layer BOPLA: exactly the five caller-editable fields; `required: true` ×5 on `CreatePlanInput`, `required: false` ×5 on `UpdatePlanInput`; server-controlled columns structurally inexpressible on the wire. (The TS-side input contract is enforced by `PlanSubmitInput`/`PlanUpdateInput` at the service signature — the correct split; Pothos input refs are structural by design.)
- `backend/graphql/pothos/shared/datetime.pothos.ts`: single `scalarType("DateTime", …)` registration; the only `interface` declaration is the sanctioned `V3DefaultScalars` module augmentation (the 3.1-documented builder-free scalar wiring), not a domain type definition.
- Grep `^\s*(type|interface|enum) [A-Z]` across all three files → only the datetime module augmentation hit. **No local type definitions.**
- `plan.types.test-d.ts` still meaningful: included in the tsgo project graph (tsconfig `include: ["**/*.ts", …]`; no test-d exclusion; sub-loop per-file tsgo run reports the file compiled clean) — the compiler exercises all 12 assertions.

## Check 3 — Codegen artifacts — PASS

- Both artifacts **git-tracked**: `frontend/graphql/generated/schema.graphql`, `frontend/graphql/generated/gql/graphql.ts` (via `git ls-files`).
- **Drift check**: re-ran `bun run generate:gqlSchema && bun run codegen` at the review HEAD:
  - `[generate:gqlSchema] wrote …/schema.graphql (4151 bytes)` + codegen `✔ Generate to ./frontend/graphql/generated/gql/graphql.ts`
  - `git status --porcelain frontend/graphql/generated` → **empty**; `git diff --stat frontend/graphql/generated` → **empty** ⇒ byte-identical regen ⇒ **ZERO drift**, no manual edits downstream of the generators.
- Fresh `schema.graphql` sanity: `type Plan` carries exactly the ten REQ-060 contract fields (`id: ID!`, `title/price/currency: String!`, `sessionCount/intervalDays: Int!`, `isActive: Boolean!`, `deactivatedAt: DateTime` nullable, `createdAt/updatedAt: DateTime!`), sorted emission order, real `DateTime` scalar.
- **Exclusions confirmed**: `tsconfig.json` exclude → `frontend/graphql/generated` (tsgo); `oxlint.config.mts` ignorePatterns → `frontend/graphql/generated/**`; `biome.json` → `"!frontend/graphql/generated"`.
- `codegen.ts` document globs (`sharedDocuments/**/*.ts` + `frontend/views/**/*.documents.ts`) unchanged and satisfied.

## Check 4 — i18n type layer — PASS

- `shared/locale/types/plans/index.ts`: `PlansLabels` counts **47 keys** (3 page-header + 12 catalog-table incl. the 4.4 amendment keys `columnDeactivatedAt`/`columnCreatedAt` + 2 chips + 15 form-dialog + 5 status-confirm + 5 toasts (4 interpolated, 1 plain) + 5 empty/error). Interpolated keys take `(planTitle: string) => string`; everything else `readonly string`.
- Parity suite `shared/locale/plans-namespace.parity.test.ts` pins `EXPECTED_KEY_COUNT = 47` (line 48) and runs **14 pass / 0 fail / 322 expect()** at this HEAD — EN+AR parity and plan-title interpolation shape still coherent with the type layer.

## Gates run (evidence)

| Gate | Command | Result |
|---|---|---|
| Project type-check | `bun run tsgo` | exit 0 (0 errors) |
| Duplicates/quality sub-loop | `bun run scripts/health/sub-loop.ts <file> --lifecycle duplicates` for `plan.types.ts`, `plan.types.test-d.ts`, `plan.pothos.ts`, `plan-inputs.pothos.ts`, `datetime.pothos.ts`, `shared/locale/types/plans/index.ts` | exit 0 on all six (tsgo + oxlint + biome:check + lint:type-aware + check:duplicates each ✅) |
| Codegen drift | `bun run generate:gqlSchema && bun run codegen` + `git status/diff frontend/graphql/generated` | zero diff |
| Parity suite | `bun run test/scripts/run-test.ts shared/locale/plans-namespace.parity.test.ts` | 14 pass / 0 fail |

No fixes were made, so no affected application suites required re-running beyond the above.

## Findings

**Resolved in this wave: none needed.** No unambiguous defect exists inside the types/codegen scope.

**Deferred-with-owner: none** (no cross-domain defect surfaced by this review). Two non-defect observations, recorded for the record only:

1. *Observation (no action)* — the GraphQL input objects (`CreatePlanInput`/`UpdatePlanInput`) are structurally built from builder primitives rather than derived from the canonical TS input types. This is inherent to Pothos input refs; the canonical `PlanSubmitInput`/`PlanUpdateInput` enforce the same field set at the service boundary, and the SDL/service duality is the plan's documented design (2.3/3.3 outcomes). Coherence verified field-by-field during this review (5 fields, same order, same requiredness split).
2. *Pre-existing, already documented in 4.4* — server-localized `fields[].message` vs. client locale (Apollo client sends no locale header). Outside types/codegen scope; owner remains the orchestrator's Apollo-SSR/locale follow-up noted in outcome/4.4.

## Files touched by 6.1

- `ai/plans/sprint_1/dev1-005-plan-catalog-crud-admin-only/outcome/6.1-review-types-outcome.md` (this file) — **only**.
