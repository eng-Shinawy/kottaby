# Task 1.1 Outcome — Drizzle Schema Delta: Plan Lifecycle Columns

Agent: schema-delta subagent · Branch: `feat/dev1-005-plan-catalog-crud-admin-only`

## Columns added (`backend/db/schema/billing/plans.ts`)

- `isActive: boolean("is_active").notNull().default(true)` — `boolean` added to the existing `drizzle-orm/pg-core` import
- `deactivatedAt: timestamp("deactivated_at")` — `timestamp` was already imported
- NO new enums, NO new indexes; the three CHECK constraints (`plans_session_count_check`, `plans_price_check`, `plans_interval_days_check`) are untouched
- Table JSDoc gained one domain-only lifecycle sentence (no REQ/Task/Phase/path mentions)

## Push (1.1.2) — push-only

`.env` verified: `DB_PROVIDER=postgres` present (no rewrite needed). Command:

```
bun --no-env-file run scripts/dbActions/index.ts --env-file=.env push
```

Push log tail:

```
[✓] Pulling schema from database...
[✓] Changes applied

✓ Success
```

## Test result (1.1.TE)

`bun run test/scripts/run-test.ts backend/db/test/logic/billing/plan-catalog-schema.test.ts`
→ **7 pass / 0 fail, 14 expect() calls** (one file).

Pattern source: `backend/db/test/logic/teachers/applicant-lifecycle.test.ts` — `runInRollback` + `tx` propagation to every direct Drizzle query; error assertions via `expectRepoError` try/catch (no `expect(...).rejects.toThrow()` anywhere).

1. information_schema: `is_active` `is_nullable='NO'`, `column_default` matches `/true/`
2. information_schema: `deactivated_at` `is_nullable='YES'`, `column_default` null
3. Backfill probe: zero rows with `is_active=false` OR `deactivated_at` NOT NULL (covers seeded rows)
4. Boundary: direct INSERT `session_count=0` → pg `23514` + constraint `plans_session_count_check`
5. Boundary: direct INSERT `price=-0.01` → pg `23514` + constraint `plans_price_check`
6. Chaos entry-point: direct INSERT `interval_days=0` → pg `23514` + constraint `plans_interval_days_check`
7. Valid minimal insert → server-side defaults `isActive=true`, `deactivatedAt=null`, `currency='EGP'`

## Quality loop (1.1.QL) — `--lifecycle duplicates`

- `plans.ts`: ✅ All checks passed — EXIT=0 (lint:type-aware + jscpd intra-file)
- `plan-catalog-schema.test.ts`: two mechanical failures found and fixed in-place, final ✅ EXIT=0:
  1. tsgo TS2352 — `Error` → `Record<string, unknown>` cast in `pgErrorCarries`; fixed with `in`-narrowing branch per key (`"code" | "constraint"`)
  2. oxlint `no-unsafe-type-assertion` — `rows as PlanColumnRow[]`; fixed with runtime type guards (`isRecord`/`extractRows` + `flatMap` defensive mapping), zero casts

## Type-check delta (1.1.SR)

`bun run tsgo 2>&1 | grep -c "error TS"` → **0** (baseline 0). Sub-loop tsgo stage also passed for both files individually.

## Security & tenancy audit (1.1.SEC)

- **Non-destructive backfill**: `is_active boolean NOT NULL DEFAULT true` fills all pre-existing rows with `true` — zero data loss; proven live by the backfill probe (0 drifted rows incl. seeded catalog rows).
- **No new writable surface**: columns are server-controlled catalog state; no repo/service/GraphQL/DTO changes in this phase, so no client can address them. Test writes are direct Drizzle inserts inside forced-rollback transactions, not API paths.
- **No permission/enum surface change**: no new enums, no new indexes, CHECK constraints unchanged and still enforced (boundary probes pass against the live DB).

## DATABASE_MIGRATIONS.md compliance (1.1.IV)

> **Note:** `db reset` and `db cleanGenerate` are permanently disabled by repo policy. Use `db push` for schema changes and `db migrate` for migration management. (docs/DATABASE_MIGRATIONS.md:26)

Also: "`bun db push` — Apply schema changes directly (no migration journal)" (docs/DATABASE_MIGRATIONS.md:147). Push-only path used; no custom SQL; nothing added under `backend/db/migration/` or `backend/drizzle/`.

## Carry-forward knowledge (for 1.2, 2.1–2.3)

- `plans.ts` import delta: `boolean` added to `drizzle-orm/pg-core`; `timestamp` already present.
- Test pattern: `runInRollback`/`expectRepoError` from `@/backend/db/test/test-utils`; `DBTransaction` from `@/backend/types`. The test-utils `db` uses the same `DATABASE_URL` (app_db) the push targets, so information_schema probes see pushed changes immediately.
- Drizzle wraps driver errors (`DrizzleQueryError.cause` chain); raw pg errors carry `code` (`"23514"` = check_violation) and `constraint` (exact CHECK name) — traverse with `in`-narrowing, NOT `as Record<string, unknown>` (triggers tsgo TS2352) and NOT `x as Target[]` narrowing casts (triggers oxlint `no-unsafe-type-assertion`).
- `tx.execute(sql\`…\`)` result shape is driver-dependent (bare array vs `{ rows }` envelope) — normalize defensively.
- Environment: `/home/z/.bun/bin/bun` was missing (sandbox reset) while the `test/scripts/run-test.ts` wrapper spawns that exact path; fixed with a symlink to `/usr/local/bin/bun` (repo untouched). Re-check after future sandbox resets.
- `$inferSelect`/`$inferInsert` now flow `isActive`/`deactivatedAt` (tsgo-clean typed `.returning()` access in the new test) — the guarded conditional UPDATE pattern (D2 / REQ-014/015) can reference `plans.isActive` directly in 2.2.3/2.3.4.

## Files NOT modified (and why)

- `backend/db/repo/**`, `backend/services/**`, `backend/graphql/**`, `backend/types/**` — no API surface in this phase; columns server-controlled only
- `backend/db/migration/**`, `backend/drizzle/**` — push-only ruling; no custom SQL
- `.env` — already `DB_PROVIDER=postgres`; no rewrite
- `deferred-items.md` — no blockers; both sub-loop mechanical failures fixed in scope (D1/D2/D3 untouched)
- `frontend/**` — out of scope for 1.1

## Semantic review attestation (1.1.SR)

- Schema change + runtime-adjacent test landed in the same commit set (no drift); no raw SQL migration.
- No dead branches; no cross-layer imports (test imports only bun:test, drizzle-orm, the schema file, and test-utils); no enum changes (nothing to value-import); comments domain-only.
- Scope boundary respected: only `plans.ts`, the new test file, this outcome, `tasks.md` checkboxes, and `worklog.md` were touched.
