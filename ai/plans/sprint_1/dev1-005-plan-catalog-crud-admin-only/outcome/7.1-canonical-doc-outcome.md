# Task 7.1 Outcome — Canonical Doc `docs/billing/plan-catalog.md`

**Task ID:** 7.1 (DEV1-005 — Plan Catalog CRUD (Admin Only), REQ-080)
**Agent:** general-purpose (Z.ai Code)
**Date:** 2026-08-26

## Deliverables

| Artifact | Status |
|---|---|
| `docs/billing/plan-catalog.md` (NEW — first file under `docs/billing/`) | Created |
| `worklog.md` — `Task ID: 7.1` section | Appended |
| Production/test code | **Untouched** (no code changes, no build, dev server untouched) |

## Document Structure (REQ-080)

Section list as shipped (Why → Pattern → Rules → What NOT to Do → Rollout Summary → Related Documents, per the REQ-080 standard structure, specialized to the task brief):

1. **Why** — FR-2.1 (admin-exclusive CRUD via field-level authScopes), FR-2.2 (title-encoded varieties, NO `plan_type` column), FR-2.3 (verification plan = ordinary `sessionCount = 5` row).
2. **Lifecycle Columns and the Pair Invariant** — `is_active NOT NULL DEFAULT true` + `deactivated_at` nullable; invariant `is_active = (deactivated_at IS NULL)`; push-only delta; no-index ruling.
3. **Pattern: Guarded State-Transition (`setActiveStatusOnce`)** — D2 conditional `UPDATE … WHERE id AND is_active = NOT target RETURNING *`; TOCTOU = 0; idempotency rejects (`PLAN_ALREADY_ACTIVE`/`PLAN_ALREADY_INACTIVE`) never silent no-ops; `null` result → `existsById` probe → `PLAN_NOT_FOUND` (D3).
4. **Visibility Split (`planCatalog` vs `adminPlans`)** — D5 two-operation split; field-level scope enforcement before resolver body (BFLA); single active predicate in `listActive`; consumer surface server-filtered ONLY (zero filter args).
5. **Forward-Only Edits and the No-Price-Snapshot Trade-off** — 5-field whitelist; INV-PC2 preservation pointer (outcome 5.1); REQ-018 forward-only semantics; the trade-off (no price-snapshot column in MVP) and its revisit trigger (first renewal/auto-billing/historical-invoicing flow); REQ-045 last-write-wins note.
6. **Error Code Map** — table of wire codes (`UNAUTHORIZED`, `FORBIDDEN`, `VALIDATION`+`fields[]`, `PLAN_*_INVALID` top-level with class VALIDATION per Ruling B, `PLAN_PATCH_EMPTY`, `PLAN_NOT_FOUND`, `PLAN_ALREADY_ACTIVE/INACTIVE`, masked `INTERNAL_SERVER_ERROR`) → meaning → producer; 23514 check-violation translation + duplicate-title (no 23505) note.
7. **Consumption Guides** — DEV1-006 (listActiveCatalog lookups; D2-deferred purchase-time re-validation contract `is_active = true` inside the purchase transaction; tx-last composition), DEV2-005 (stable-title lookup rule + `sessionCount = 5`; `VERIFICATION_PLAN_TITLE` constant; service-bootstrap-only seeding; no lifecycle repair on re-run), DEV1-009 (tx-last signatures, thin resolvers, `runInRollback` test discipline pointer to the billing suites).
8. **What NOT to Do** — catalog-ID non-sensitivity ruling NOT inheritable by user-linked/sensitive resources (REQ-032 caveat); `escapeLikeWildcards` mandatory for any future search input; `title` is DATA, not an i18n key (UI copy = `plans` namespace, 47 keys); no delete (INV-PC3); no forked visibility predicate; no lifecycle mutation through the edit path.
9. **Rollout Summary** — artifact pointers (schema, repo, service, GraphQL surface, admin UI, seed, spec addenda).
10. **Related Documents** — relative links to real files only.

## Sourcing

Source material: `plan.md` D1–D8 decisions table, `specs.md` REQ-010/014/015/018/019/032/034/035/040/043/045/080, outcome files 5.1/5.2/6.4, `deferred-items.md` D1/D2, worklog waves 0.1→6.5, and the shipped implementation files (read in full: `plan.repository.ts`, `seed-plan-catalog.ts`; signatures/doc-headers: `plan-catalog.service.ts`, resolvers, schema).

Style mirrored on `docs/graphql/domain-error-extensions-code.md` (Why → Pattern → Rollout Summary → Related Documents; relative-link Related Documents list).

## Gates

| Gate | Result |
|---|---|
| Doc lint | **No docs-lint tool exists in the repo** (no `lint:docs`/markdownlint script in `package.json`, no markdownlint config) — absence noted; nothing to run. Markdown is plain (no mermaid, so `validate:mermaid` N/A). |
| Link verification | Script-checked all 38 relative links in `docs/billing/plan-catalog.md` → **all resolve to real files** (docs siblings, backend sources, AI plan artifacts, test suites). |
| `bun run tsgo` | **0 errors** — doc-only change; type graph unaffected. |
| Code touch | None — diff is exactly: new `docs/billing/plan-catalog.md`, new outcome file, `worklog.md` append. |

## Deviations / Notes

- The brief's `seeds/billing/seed-plan-catalog.ts` resolves to the real path `backend/db/seeds/billing/seed-plan-catalog.ts` — the doc references the real path (link verified).
- `docs/specs/state-machine-invariants.md` does not yet contain the INV-PC1..PC3 section at this HEAD (owned by the plan's knowledge-propagation task 7.2); the doc links the file and words the pointer so it stays true whether read before or after 7.2 lands.
- Deferred items unchanged: D1 → DEV3-020 (audit integration; the doc documents `emitPlanAuditSeam` as hook-point-only), D2 → DEV1-006 (purchase-time re-validation; the doc records it as an explicit deferred obligation in the DEV1-006 guide).
