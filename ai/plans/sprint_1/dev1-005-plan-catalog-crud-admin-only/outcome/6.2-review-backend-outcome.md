# Task 6.2 — Review Wave: Backend (repository, service, resolvers, error contracts)

- **Reviewer**: review-backend (Task 6.2)
- **Scope**: REQ-011..018, REQ-040..045, REQ-050..053 — `backend/db/repo/billing/plan.repository.ts`, `backend/services/billing/plan-catalog.service.ts`, `backend/graphql/query/billing/plan-catalog.query.ts`, `backend/graphql/mutation/plan-catalog.mutation.ts`, `backend/graphql/pothos/billing/plan-inputs.pothos.ts`, DB test suites under `backend/db/test/logic/billing/`.
- **Baseline**: local `main` HEAD `79bfb70` (6.1/6.3/6.4 waves landed, zero fixes). Deliverable branch: `origin/feat/dev1-005-plan-catalog-crud-admin-only`.
- **Verdict: PASS (1 comment-only defect fixed in scope; 1 deferred-with-owner carried from 6.4).**

---

## Check 1 — PlanRepository (guarded-UPDATE primitives, single active predicate, no domain errors): PASS

| Sub-check | Verdict | Evidence |
|---|---|---|
| Only state-transition mechanism | PASS | Repo-wide grep `update(plans)` / `UPDATE plans` / `delete(plans)` / `DELETE FROM plans` across `backend/**`: the ONLY production UPDATEs on `plans` are `updatePlanFields` (repo :115-116) and `setActiveStatusOnce` (repo :145-153). The only INSERTs are `insertPlan` (repo :81-82) and test fixtures (`entity-setup.ts`, schema-test scaffolding). **Zero DELETE surfaces** (INV-PC3 holds at the data layer). |
| Guarded predicate | PASS | `setActiveStatusOnce` WHERE = `id = $1 AND is_active = !target` (repo :147/:152) evaluated under the row write lock inside one statement; loser gets `rows[0] ?? null`. 5.2's double-deactivation probe already proved exactly-once at the wire. |
| Single-predicate active filter | PASS | `listActive` is the sole visibility predicate (`is_active = true` — raw SQL :63 and tx-branch Drizzle :182). Repo-wide `isActive|is_active` grep over non-test backend files: all other hits are the schema default, the transition guard, docs, and the wire arg of `setPlanActiveStatus`. `listAll`/`listForAdmin` add no filter; service adds no predicate of its own. |
| Whitelist mapping (no spreads of client input) | PASS | `updatePlanFields` builds `set` via per-key conditional copies of the `PlanFieldPatch` type (:106-113); `PlanFieldPatch` = `Partial<Pick<PlanInsertType, title|sessionCount|price|currency|intervalDays>>` — lifecycle/identity keys are compile-excluded AND runtime-skipped. Service-side `whitelistPlanPatch` (:292-300) mirrors it key-by-key. |
| No-cascade guarantee | PASS | Repo touches only `plans`; service imports zero subscription/payment/student/wallet/teacher-table modules (header discipline grep-verified). 5.1's byte-identical-ledger proof re-confirmed by suite run. |
| tx-last signatures | PASS | All five repo methods and all five service methods carry `tx?: DBTransaction` as the LAST parameter; resolvers are tx-free (thin-delegation design). |
| Repository raises NO domain errors | PASS | Repo imports no `@/backend/lib/errors`; no try/catch anywhere — a 23514 (or any driver error) propagates raw to the caller. The only throw is the defensive generic `Error` in `insertPlan`'s zero-row branch (:85), unreachable for a single-row INSERT and explicitly NOT a domain error. |

## Check 2 — PlanCatalogService (validation before write, error map, localization, logging): PASS

| Sub-check | Verdict | Evidence |
|---|---|---|
| Validation before write | PASS | `createPlan`: `validatePlanInput` (requireAll) before `insertPlan` (:391-407). `updatePlan`: empty-patch reject → `collectPlanFieldErrors` before `updatePlanFields` (:437-454). `setPlanActiveStatus`: `parsePlanId` before the guarded transition (:488). Zero reads-then-writes; writes are single statements. |
| Error code map | PASS | Aggregated `ValidationError` (code `VALIDATION`) with `fields[]` = `{field, code, message}` per offending field (:268-273); `NotFoundError(PLAN_ENTITY, …)` auto-codes `PLAN_NOT_FOUND` (errors.ts :39); `ConflictError("PLAN_ALREADY_ACTIVE"/"PLAN_ALREADY_INACTIVE", …)` for idempotency conflicts (:497-499); Ruling B honored — `translatePlanPersistenceError` maps recognized 23514 constraint names → `ValidationError(PLAN_*_INVALID, localized)` (:355-366) and RETHROWS unknown constraints untranslated (:364-365) for the masking boundary. Error-class code behavior verified against `backend/lib/errors.ts`. |
| Localization paths everywhere | PASS | Every caller-facing message resolves through `getServerTranslations(locale).errorsTranslations` — `planTitleRequired/planTitleTooLong/planSessionCountInvalid/planPriceInvalid/planCurrencyInvalid/planIntervalDaysInvalid/planPatchEmpty/planNotFound/planAlreadyActive/planAlreadyInactive`. `PLAN_FIELD_ERROR_MESSAGES` maps each machine code to its translation arrow (:99-106). No hardcoded user-facing copy; the only English literals are structured log lines (operator-facing, by design). |
| Logging discipline | PASS | Expected rejections → `logPlanRejection` → `logger.logDomainError(msg, {code, entity, entityId?})` — payload limited to id + code, no values/SQL (:112-118). Unexpected DB failures → `logger.error("Plan persistence failed with an unexpected database error")` static string, then rethrow (:364). Successes → `logger.info` audit seam with `{code, entityId}` (:126-128). **`console.*` grep over all billing backend files: 0 matches.** |
| Idempotency rejects, never silent no-ops | PASS | `setPlanActiveStatus` disambiguates `null` via `existsById` → `PLAN_NOT_FOUND` vs `ConflictError` (:491-499). |

## Check 3 — Resolvers + Pothos inputs (thin delegation, scopes, coercion): PASS

| Sub-check | Verdict | Evidence |
|---|---|---|
| Thin delegation only | PASS | All five resolvers: optional `ctx.user` narrowing guard (no-non-null-assertion rule) → straight service call with `ctx.locale`. Zero business logic, zero repository imports, no try/catch (domain errors propagate uncaught to the masking boundary). |
| `$all` scope conjunctions | PASS | `planCatalog` = `$all{authenticated}`; `adminPlans`, `createPlan`, `updatePlan`, `setPlanActiveStatus` = `$all{authenticated, role:[UserRole.Admin]}` (query :51-55/:81-86; mutation :58-63/:94-99/:140-145). Plain-map ANY-semantics trap documented in both file headers; 401/403 split wire-proven by 3.2/3.3/3.6 suites and re-proven by 6.4. |
| Locale propagation | PASS | Every service call carries `ctx.locale` (query :66/:95; mutation :80/:127/:152). |
| `Number(id)` coercion safety | PASS | `ID` arrives as string → `Number(args.id)` → service `parsePlanId` re-validates `Number.isInteger && > 0`, else localized `planNotFound` ValidationError (service :135-141). All queries parameterized (`$1` placeholders / Drizzle `eq`) — no injection surface regardless of coercion outcome. |
| Input BOPLA (SDL layer) | PASS | `CreatePlanInput` = exactly the 5 commercial fields all REQUIRED; `UpdatePlanInput` = same 5 all OPTIONAL; `id/isActive/deactivatedAt/createdAt/updatedAt` structurally unrepresentable; `price` is `t.string` (REQ-013 no-Float). Mutation resolver maps input field-by-field; partial-patch builder normalizes explicit null → "not supplied" (mutation :112-123). |

## Check 4 — DB test discipline (six suites spot-verified): PASS

| Suite | runInRollback + tx | Fixtures | expectRepoError |
|---|---|---|---|
| plan-catalog-schema.test.ts | YES (:117-207, tx to every direct call) | Direct inserts (sanctioned exemption — suite probes table structure/CHECKs, documented in header) | YES (:158/:174/:192) |
| plan-catalog.repository.test.ts | YES (header + all tiers) | `createTestPlan` / `createTestPlanWithSubscription` (collision-proof titles) | YES |
| plan-catalog.service.test.ts | YES (every case) | entity-setup only | YES (try/catch wrappers :85-112) |
| plan-catalog-seed.test.ts | YES (double bootstrap in-rollback, tx to every service call) | Seeder itself (scoped relative to dev-DB baseline — documented) | n/a (no rejection probes) |
| plan-catalog-preservation.test.ts | YES (tx to every service/entity-setup/direct-Drizzle call) | `createTestPlanWithSubscription` + direct student_payments ledger lane | n/a (documented — rejection probes live in the service suite) |
| plan-entity-setup.test.ts | YES (post-rollback residue probe is the only deliberate pool read, read-only) | The helpers themselves | n/a |

- No suite depends on seed data for fixtures (schema suite's global backfill quantifier concern was 5.3's environmental note; at this HEAD the full billing dir is green — see gates below).
- Ruling A honored in reverse: only the service suite uses module-scope `getServerTranslations("en"/"ar")` as its assertion oracle (sanctioned); every other suite uses literal `"en"` per Rule 19.

## Check 5 — REQ-040..045 spot-checks: PASS

- **Duplicate-title tolerance (REQ-040)**: no unique key — repo test "duplicate titles are tolerated" (:276), service test "duplicate titles both succeed — double-submit tolerance" (:634, two distinct ids).
- **Title 255-after-trim (REQ-041)**: service matrix — empty/whitespace-only rejected, `"a".repeat(255)` passes, `"b".repeat(256)` rejected (:291-307); create/patch both trim before persisting.
- **Price decimal-string pattern (REQ-042)**: `"0.00"`, `"99999999.99"` pass; `"-0.01"`, `"abc"`, `"1.005"`, `"100000000.00"` rejected with `PLAN_PRICE_INVALID` (:328-347).
- **Currency 3-uppercase (REQ-043)**: uppercase 3-letter passes; lowercase (`"egp"`) and short codes rejected with `PLAN_CURRENCY_INVALID` (:350+, :240).
- **intervalDays ≥ 1 (REQ-045)**: `0`/negative/non-integer rejected at the service; DB-layer CHECK (`interval_days <= 0`) enforced at the repo (:333) and schema suites (:197).
- **Idempotency rejects (REQ-014/015)**: `PLAN_ALREADY_ACTIVE` / `PLAN_ALREADY_INACTIVE` asserted as ConflictError in both directions, never silent no-ops (:213/:224/:461/:669); localized EN/AR switch test present (:644-668).

---

## Findings

| ID | Severity | Status | Detail |
|---|---|---|---|
| F-6.2-1 | Minor (doc drift) | **RESOLVED in this wave** | `backend/db/repo/billing/plan.repository.ts:5` header claimed "`title` (unique)" — contradicted the duplicate-title tolerance ruling (REQ-040), the file's own `insertPlan` doc (:73-75), and the live schema (pg_constraint = pkey + 3 CHECKs, verified by 6.4). Comment-only fix: now reads "`title` (deliberately NOT unique — duplicate-title tolerance ruling, REQ-040)". No behavior change. |
| D-6.2-1 | Cosmetic | DEFERRED (owner: next docs sweep) | Identical stale "`title (unique)`" wording at `backend/db/schema/billing/plans.ts:5` — already logged by 6.4 as D-6.4-2; the schema file is outside 6.2's fix scope, so left untouched for consistency with that ruling. Doc-only; no runtime effect. |

No other defects found in scope. Zero assertion changes in any test file.

## Gates (post-fix)

- `bun run tsgo` → **0 errors** (exit 0).
- `bun run scripts/health/sub-loop.ts backend/db/repo/billing/plan.repository.ts --lifecycle duplicates` → **exit 0** (tsgo/oxlint/biome/lint:type-aware/check:duplicates).
- `bun run test/scripts/run-test.ts backend/db/test/logic/billing/` → **64 pass / 0 fail / 382 expects across 6 files** (schema, repository, service, seed, preservation, entity-setup — all green at this HEAD; repository suite re-run standalone: 17/17 / 65 expects).
- No build, dev server :3000 untouched, Postgres :5432 UP. Not touched: `frontend/**`, `backend/types/**`, tests' assertions.
