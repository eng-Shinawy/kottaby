# Task 1.4 Outcome — i18n: New `plans` UI Namespace (Full Registration Procedure)

Status: COMPLETE · tsgo parity delta 0 (baseline 0) · sub-loop EXIT=0 on all 10 files · parity test 14 pass / 0 fail · no git commits.

## What was built

The `plans` UI namespace registered end-to-end through the compile-time translation system: `PlansLabels` interface → EN/AR leaf implementations → `plansTranslations` on the `Translations` interface → aggregation into both message bundles → `Plans` handle via `defineNamespace` → namespaces registry export → structural parity test.

## Full key inventory — `PlansLabels` (45 keys, grouped by surface)

| Surface | Keys |
|---|---|
| Page header (3) | `pageTitle`, `pageSubtitle`, `createButton` |
| Catalog table (10) | `columnTitle`, `columnSessionCount`, `columnPrice`, `columnIntervalDays`, `columnStatus`, `columnActions`, `actionEdit`, `actionActivate`, `actionDeactivate`, `loading` |
| Status chips (2) | `statusActive`, `statusInactive` |
| Create/edit dialogs (15) | `createDialogTitle`, `editDialogTitle`, `fieldTitle`, `fieldTitlePlaceholder`, `fieldSessionCount`, `fieldSessionCountPlaceholder`, `fieldPrice`, `fieldPricePlaceholder`, `fieldCurrency`, `fieldCurrencyPlaceholder`, `fieldIntervalDays`, `fieldIntervalDaysPlaceholder`, `save`, `cancel`, `submitting` |
| Status-confirmation dialog (5) | `deactivateConfirmTitle`, `deactivateConfirmBody(planTitle)`, `activateConfirmTitle`, `activateConfirmBody(planTitle)`, `confirm` |
| Toasts (5) | `toastCreated(planTitle)`, `toastUpdated(planTitle)`, `toastActivated(planTitle)`, `toastDeactivated(planTitle)`, `toastActionFailed` |
| Empty/error states (5) | `emptyStateTitle`, `emptyStateBody`, `errorStateTitle`, `errorStateBody`, `errorStateRetry` |

Interpolation style: the six title surfaces are dashboard-pattern function closures `(planTitle: string) => string` (mirrors `DashboardLabels.welcome`); all plain strings are brace-free (no ICU coupling), verified by the parity suite. AR copy is RTL-native phrasing (e.g. «حفظ», «تنشيط», «إيقاف», «كتالوج الباقات»), tone-matched to the existing AR leaves (`dashboardAr` "الباقات"); no copy length is encoded in any consumer contract (no widths shipped), placeholders expand at runtime.

## Files created (6)

- `shared/locale/types/plans/index.ts` — `PlansLabels` interface (45 readonly keys, JSDoc per key)
- `shared/locale/en/plans/index.ts` — `plansEn: PlansLabels`
- `shared/locale/ar/plans/index.ts` — `plansAr: PlansLabels`
- `shared/locale/namespaces/plans/plans.namespace.ts` — `export const Plans = defineNamespace<PlansLabels>("plans.plans", translations => translations.plansTranslations)`
- `shared/locale/namespaces/plans/index.ts` — `export * from "./plans.namespace"`
- `shared/locale/plans-namespace.parity.test.ts` — structural parity + wiring suite (mirrors `applicant-namespace.parity.test.ts`)

## Files modified (4) — Edit-tool surgical anchors only

- `shared/locale/types/message.ts` — `import type { PlansLabels }` + `plansTranslations: PlansLabels` on `Translations`
- `shared/locale/en/messages.ts` — `plansEn` import + `plansTranslations: plansEn` on `enMessages: Translations`
- `shared/locale/ar/messages.ts` — `plansAr` import + `plansTranslations: plansAr` on `arMessages: Translations`
- `shared/locale/namespaces/index.ts` — `Plans` import, `export * from "./plans"`, `Plans` added to the `namespaces` registry const

## Registration steps performed (order)

1. Authored `PlansLabels` (`shared/locale/types/plans/index.ts`).
2. Added `plansTranslations: PlansLabels` to the `Translations` interface (`types/message.ts`).
3. Implemented `plansEn` / `plansAr` leaf consts typed against `PlansLabels`.
4. Aggregated both consts into `enMessages` / `arMessages` (typed `Translations` — a missing key fails `bun tsgo`).
5. Created the `Plans` handle (`defineNamespace<PlansLabels>("plans.plans", …)` pattern per `namespaces/dashboard/`) + directory barrel.
6. Exported through `namespaces/index.ts` (import + `export *` + registry const entry).
7. Server namespace-path registration: the amended task text's "Locale server namespace-path registration map" resolves to `messagesByLocale` in `shared/locale/server.ts`, which is populated transitively by `enMessages`/`arMessages`. Grep verified NO separate string-path map exists (`applicant.applicant`/`dashboard.dashboard` ids appear only inside `shared/locale/namespaces/` and the parity tests) — so no additional file required; `getTranslations(locale)` / `getServerTranslations(locale)` now serve `plansTranslations` for both locales.

## Verification outputs

- `bun run tsgo 2>&1 | grep -c "error TS"` → **0** (baseline 0; run after all 10 files were in place).
- Sub-loop `--lifecycle duplicates` EXIT=0 on every created/modified file (10/10): `types/plans/index.ts`, `en/plans/index.ts`, `ar/plans/index.ts`, `namespaces/plans/plans.namespace.ts`, `namespaces/plans/index.ts`, `types/message.ts`, `en/messages.ts`, `ar/messages.ts`, `namespaces/index.ts`, `plans-namespace.parity.test.ts`. Two in-place fixes during the loop: unsafe type assertions in the test replaced with `typeof`-narrowed helpers; sonarjs `prefer-specific-assertions` → `expect(arKeys).toHaveLength(...)`.
- `bun run test/scripts/run-test.ts shared/locale/plans-namespace.parity.test.ts` → **14 pass / 0 fail, 310 expect() calls**. Locks: (1) ar/en key-set equality at exactly 45 keys + non-empty values; (2) per-key value-kind parity (string vs formatter) + brace-free plain strings + single-sentinel expansion of all six title closures in BOTH locales + interpolator set is exactly the six; (3) registry wiring (`namespaces.Plans`, id `plans.plans`, getter resolves both bundle slices, `plansTranslations` on both bundles) + server lineage (`getTranslations("en"|"ar").plansTranslations` identity) + `useAppTranslation(Plans)`-compatible handle shape. Client-hook runtime proof stays Phase 4's job.

## 1.4.SEC — Security & Tenancy Audit

Plan **content** (`plans.title`, e.g. "Tajweed twice a week") is admin-authored **DATA** persisted in the `plans` table — it is NOT part of the translation key space. The 45 keys are fixed UI chrome only; admin titles enter UI strings solely through the runtime `{planTitle}`-equivalent closure argument. No admin content can mint or collide with namespace keys; no key-space pollution. Toast/confirm copy contains no internal identifiers, constraint names, or SQL fragments.

## 1.4.SR — Semantic Review

Property-access convention only (`t.plansTranslations.*` / handle-typed `t.*`); the `Plans` handle is the sole client access mechanism per the amended task text (import path verified by Phase 4); zero hardcoded UI strings in components (none exist yet — Phase 4 consumers must import the handle); `shared/` purity holds: new files import only from `@/shared/locale/**` (AGENTS.md path-alias discipline), zero imports from `@/frontend|backend|app`, no `next-intl`, no `shared/messages/` references.

## 1.4.IV — Instruction Verification (`shared/locale/AGENTS.md`, quoted)

- "All imports inside `shared/locale/**` MUST use the `@/shared/locale/...` absolute alias. Relative `../../types/X` traversals are prohibited." → all six files use `@/shared/locale/...` exclusively. ✅
- "Each leaf file exports a single `auth<Scope>Ar` / `auth<Scope>En` (or similar) const typed against the matching `*Labels` interface from `shared/locale/types/...`." → single `plansEn`/`plansAr` consts typed `PlansLabels`. ✅
- "Leaf modules contain **no logic** — only plain string/object literals." → closures are pure literals; no I/O, no branching. ✅
- "Interpolation uses the ICU `{var}` format consumed by the runtime formatter" — conscious, documented deviation consistent with the cited pattern namespace: `DashboardLabels` (the amended text's reference pattern) uses typed function closures for interpolation (`welcome: (userName: string) => string`); `plans` follows the Dashboard convention, keeping UI interpolation type-safe without the formatter, and the parity suite pins closure shape/behavior in both locales. Plain strings remain brace-free so the ICU formatter contract is untouched. ✅ (documented)
- Registration procedure (Translations interface entry → bundle aggregation → `defineNamespace` handle → registry export) matches the exact shape of the six pre-existing namespaces. ✅

## Carry-forward knowledge for Phase 4 (Tasks 4.2–4.5)

```ts
// Client components:
import { Plans } from "@/shared/locale/namespaces/plans"; // also re-exported from @/shared/locale/namespaces
const t = useAppTranslation(Plans);
t.pageTitle; t.createButton; t.columnStatus; t.statusActive; t.fieldTitlePlaceholder;
t.deactivateConfirmBody(plan.title); t.toastCreated(plan.title); t.toastActionFailed;

// Server Components / API:
import { getServerTranslations } from "@/shared/locale/server-graphql"; // or getTranslations from @/shared/locale/server
const translations = getServerTranslations(locale);
translations.plansTranslations.pageTitle;
```

Handle id is `"plans.plans"` (registry key `Plans`); getter returns the `PlansLabels` slice, so every key above is available with full autocompletion and compile-time parity. The 45-key contract covers every Phase-4 surface listed in Task 1.4.1 — if a Phase-4 surface needs a new key, add it to `PlansLabels` + both leaves + bump `EXPECTED_KEY_COUNT` in the parity test (tsgo makes the two leaves non-skippable).

## Cross-file dependencies

- **Consumed by:** Task 4.2 (catalog page/table), 4.3 (create/edit dialogs + toasts), 4.4 (status confirmation + activate/deactivate toasts), 4.5 (empty/error/feedback states).
- **Built on:** Task 1.3's `errors.planCatalog` group is independent (separate `errors` namespace slice) — no file overlap; `types/message.ts`, `en|ar/messages.ts`, `namespaces/index.ts` edits were anchored so both tasks' changes compose (Task 1.3 only extends the `errors` leaf/type files).

## Files NOT modified (and why)

- `shared/locale/types/index.ts` (types barrel) — existing convention: `applicant`/`auth`/`errors`/`recitation` label types are NOT re-exported there either; consumers import `@/shared/locale/types/plans` directly. Avoids a second shared-barrel edit Task 1.3 may race on.
- `shared/locale/server.ts` / `server-graphql.ts` — no per-namespace string-path map exists (verified by grep); both already serve the full `Translations` object once bundles aggregate the new slice.
- `shared/locale/AppLocale.ts`, `localeContext.tsx`, `client/*` — locale enumeration and hook mechanics are namespace-agnostic.
- `shared/locale/types/errors/**`, `en/errors/**`, `ar/errors/**`, `errors-namespace.parity.test.ts` — Task 1.3's exclusive territory (concurrent subagent).
- Any `frontend/**`/`backend/**` file — Phase 1 is wiring-free by design; consumers land in Phase 4.
