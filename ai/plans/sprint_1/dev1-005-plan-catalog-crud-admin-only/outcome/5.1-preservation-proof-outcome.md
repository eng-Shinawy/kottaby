# Task 5.1 — Deactivation Preservation Proof (REQ-075) — Outcome

**Plan:** ai/plans/sprint_1/dev1-005-plan-catalog-crud-admin-only/ · **Phase:** phase-5-integration-differential · **Task:** 5.1
**Status:** ✅ COMPLETE — all gates green, committed to `main`, pushed to `origin/feat/dev1-005-plan-catalog-crud-admin-only`

## Deliverable

- **NEW** `backend/db/test/logic/billing/plan-catalog-preservation.test.ts` — 3 tests / 42 `expect()` calls, all passing.
- No other file touched (service, repo, entity-setup, frontend/graphql, parallel 5.2's file all read-only).

## What was proven (REQ-017, REQ-018, REQ-075 — lifecycle independence)

Fixture (per case, inside `runInRollback`, `tx` propagated to EVERY call):
- `createTestPlanWithSubscription(tx, { student: {balanceHifz: 12, balanceTajweed: 8, balanceReviews: 3}, subscription: {…deterministic status/dates/payment metadata} })` — plan → subscription → student linkage via the 2.1 entity-setup helpers.
- **Balance lanes**: the per-student `students.balance_*` columns (the REQ-017 "credited `students.balance_*` lanes" per plan 2.1 outcome), credited NON-ZERO so any decrement/recompute breaks the proof; plus one `student_payments` row linked to the fixture subscription's student inserted directly through the Drizzle schema (the only `backend/db/schema/billing/` lane that links a student to the subscription; no helper exists — direct insert follows entity-setup idioms, deviation noted below).
- **Snapshot discipline**: FULL rows re-read via explicit SELECT (`readLedgerSnapshot`) *before* mutation, then re-read *after* mutation; asserted with full-row `toEqual` deep-equality — never selected columns (5.1.SR). Explicit balance-lane echo for legible failure output.

| # | Test | Proof |
|---|------|-------|
| 1 | `setPlanActiveStatus(id, false)` | subscription + student row + student-payment row **byte-identical** post-deactivation; catalog co-asserted changed (`isActive=false`, `deactivatedAt` non-null) while price/sessionCount/intervalDays frozen (REQ-017/075, A.9) |
| 2 | `updatePlan({price:"999.99", sessionCount:12, intervalDays:7})` | same three ledger rows **byte-identical** post-edit; catalog co-asserted re-priced, lifecycle pair untouched (REQ-018, INV-B2/B3 shield, forward-only) |
| 3 | deactivate → edit in sequence | ledger **byte-identical** across the combined lifecycle pair; catalog carries BOTH mutations; `subscription.status` stays `active` (INV-PC2) |

Anti-vacuous guards: catalog change co-asserted in every test (steps 4 of the task) — a silent no-op service call would fail the `isActive`/price/sessionCount/intervalDays assertions.

## Disciplines honored

- `runInRollback` wrapper; rollback-forced transaction; no pool path.
- `tx` (typed `DBTransaction`) propagated to every service / entity-setup / direct-Drizzle call.
- Fixtures exclusively via entity-setup helpers + one direct schema insert for the payments lane.
- Rule 19: NO `getServerTranslations` in scaffolding — literal `"en"` locale only.
- No rejection probes in this file (covered by the 2.3 service suite) → `expectRepoError` intentionally not imported (no unused imports).
- `expect(...).rejects.toThrow()` absent; no console output (service audit-seam INFO logs are the logger's own, as in the service suite).

## Gates

| Gate | Result |
|------|--------|
| `bun run test/scripts/run-test.ts backend/db/test/logic/billing/plan-catalog-preservation.test.ts` | ✅ 3 pass / 0 fail, 42 `expect()` calls (308 ms) |
| `bun run scripts/health/sub-loop.ts …plan-catalog-preservation.test.ts --lifecycle duplicates` | ✅ exit 0 (tsgo, oxlint, biome:check, lint:type-aware, check:duplicates all pass) |
| `bun run tsgo` (project-wide) | ✅ exit 0 — no errors |
| Untouched: frontend/graphql/**, service, repo, entity-setup, 5.2's file; no build; :3000 untouched | ✅ |

## Deviations

1. **No student "balance/credit" table exists in `backend/db/schema/billing/`** — the student balance lane is the `students.balance_hifz/tajweed/reviews` columns (per plan 2.1 outcome + REQ-017 wording). Proof therefore snapshots the full `students` row (credited non-zero) AND adds a `student_payments` row linked to the fixture subscription's student (the only billing/ table joining student↔subscription) inserted via direct schema insert, satisfying the task's "one row linked to the fixture subscription's student" requirement with the strongest available interpretation.
2. Subscription fixture pinned to deterministic dates/payment metadata (allowed via helper's `subscription` overrides) so the byte-identical proof covers `status`/`start_date`/`end_date`/payment columns explicitly, not just defaults.

## Next actions

- 5.2 (parallel): concurrency suite in `frontend/graphql/test/billing/plan-catalog.concurrency.test.ts` — no coordination needed; files disjoint.
- 7.2: record INV-PC2 evidence pointer to this file in `docs/specs/state-machine-invariants.md`.
