# Task 3.6 — Full GraphQL Role-Matrix Integration Proof (REQ-072) — Outcome

Plan: DEV1-005 Plan Catalog CRUD (Admin Only) · Phase: phase-3-graphql-resolvers-api · Task: 3.6
Artifact: `frontend/graphql/test/billing/plan-catalog.roles.test.ts` — 27 tests / 830 expect() calls, **all green**.
Result: **PASS — REQ-064 + REQ-072 proven cell-by-cell over the real wire boundary. This suite IS the BFLA proof.**

---

## 1. What was built

ONE comprehensive integration suite proving the full 5-surface × 5-role permission matrix for the plan-catalog GraphQL surface, over the REAL boundary (Apollo Client v4 → `/api/graphql` on the warm dev server, same harness as the 3.2/3.3 sibling suites: `setupTestServerLifecycle` guard, `testClient`, `expectMutationError`).

- **27 tests**: 25 matrix cells (5 surfaces × 5 roles — every cell a HARD assertion, no skips, no soft-asserts) + 2 dedicated visibility-split cells (static-per-role + dynamic round-trip).
- **Every failure cell** asserts `extensions.code` via the canonical `expectMutationError` helper — NEVER message-text-coupled.
- **Every success cell** asserts data shape: field values, lifecycle columns, and `__typename "Plan"` pinning (full ten-field REQ-060 shape on the consumer-surface cells via a shared `assertActiveOnlyCatalog` helper).
- **Admin success-path round-trip** rides the REAL mutations end-to-end: `createPlan` (captures id) → `updatePlan` (price-only patch, updatedAt-bump proof) → `setPlanActiveStatus(false)` (deactivation) → `setPlanActiveStatus(true)` (re-activation) — doubling as the Task 3.3 end-to-end proof and feeding the dynamic visibility cell.

## 2. Matrix results — all 25 cells with OBSERVED codes

Observed `extensions.code` per cell (exact values extracted on the wire during the green run):

| Surface \ Role | Anonymous | Student | Parent | Teacher | Admin |
|---|---|---|---|---|---|
| **planCatalog** | `UNAUTHORIZED` ✅ | ✅ active-only (deactivated absent, full REQ-060 shape) | ✅ active-only | ✅ active-only | ✅ active-only |
| **adminPlans** | `UNAUTHORIZED` ✅ | `FORBIDDEN` ✅ | `FORBIDDEN` ✅ | `FORBIDDEN` ✅ | ✅ full catalog incl. inactive (deactivated fixture + lifecycle columns asserted) |
| **createPlan** | `UNAUTHORIZED` ✅ | `FORBIDDEN` ✅ | `FORBIDDEN` ✅ | `FORBIDDEN` ✅ | ✅ success (full persisted shape, id captured) |
| **updatePlan** | `UNAUTHORIZED` ✅ | `FORBIDDEN` ✅ | `FORBIDDEN` ✅ | `FORBIDDEN` ✅ | ✅ success (price echo + untouched fields + updatedAt bump) |
| **setPlanActiveStatus** | `UNAUTHORIZED` ✅ | `FORBIDDEN` ✅ | `FORBIDDEN` ✅ | `FORBIDDEN` ✅ | ✅ success (isActive false + non-null deactivatedAt) |

Every anonymous cell observed `UNAUTHORIZED` (401 semantics — never `FORBIDDEN`); every authenticated non-admin cell on the four admin surfaces observed `FORBIDDEN` (403). The 401/403 split — i.e. BFLA denying *unauthenticated* vs *unauthorized* differently — is therefore proven per surface, per role.

## 3. Visibility-split proof (REQ-072 core)

Asserted for EVERY authenticated role, admin included:

1. **Static split (per-role matrix cells)**: the direct-insert deactivated fixture (`isActive: false` + `deactivatedAt`, the ONLY way to craft that state — the service creates plans active) is ABSENT from `planCatalog` for Student, Parent, Teacher, AND Admin cells; the whole returned list is pinned ACTIVE-ONLY (every node `isActive: true` + `deactivatedAt: null`).
2. **`adminPlans` admin-only**: admin's full-catalog cell asserts the deactivated fixture PRESENT with its lifecycle columns (`isActive: false`, `deactivatedAt` non-null ISO string); the three non-admin cells are `FORBIDDEN` — structurally impossible for any other role to reach the full catalog (plan D5 field-level split).
3. **Dynamic split (state change drives visibility BOTH directions)**: after the admin REALLY deactivates the E2E plan through `setPlanActiveStatus`, all four authenticated roles' `planCatalog` views hide BOTH inactive rows (E2E plan + fixture) while admin's `adminPlans` still lists both; after re-activation the E2E plan RE-ENTERS `planCatalog`. Catalog membership provably tracks lifecycle state over the wire — not a fixture artifact.

## 4. Gate amendments

1. **Supervisor column VACUOUS**: RBAC reality is `UserRole` = `{ Admin, Teacher, Student, Parent }` ONLY (`backend/enum/users/user-role.enum.ts`; the `user_role` pgEnum in `backend/db/schema/enums.ts` carries exactly `["admin","teacher","student","parent"]`). No `users.role` value — hence no JWT role claim — can be "supervisor". The "supervisor" that exists in the codebase is a `ManagerAccountType` / `supervisor_default` permission-group concept (manager-onboarding axis), entirely outside the `users.role` axis the Pothos `authScopes` role gates check. The plan §3.3 matrix's Supervisor row is therefore untestable-by-construction; the Parent + Teacher `FORBIDDEN` cells bracket the same non-admin axis, and the admin gate is `role: [UserRole.Admin]` — a closed allowlist, so an absent role cannot fall through. Documented instead of faked with a skip or a synthetic row.
2. **Super Admin = `UserRole.Admin`**: the plan prototype copy's "Super Admin" maps to `UserRole.Admin`, the only admin-tier role on the `users.role` axis — the Admin column IS the Super Admin column.
3. **Test location**: `frontend/graphql/test/billing/` (established server-boundary layout) defers the plan's `backend/graphql/test/` path — same amendment already documented by the Task 3.2/3.3 outcomes; this suite is Apollo-wire testing beside its model `teachers/applicant-profile.test.ts`.
4. **Run-command bypass**: `bun run test/scripts/run-test.ts` routes `frontend/graphql/test/**` to the structurally-broken 3066 path in this sandbox (Next 16 one-dev-server-per-dir lock + `.env.test` missing). Sanctioned bypass (3.2/3.3 precedent): `TEST_SERVER_EXTERNAL=1 GRAPHQL_TEST_PORT=3000 KOTTABY_TEST_RUNNER_OK=1 bun test <file>` against the warm dev server on :3000. CI keeps the standard `setupTestServerLifecycle()` boot (the guard is `if (process.env.TEST_SERVER_EXTERNAL !== "1")`).
5. **Inline documents**: `parse()` literals annotated as type-only `TypedDocumentNode`s — no codegen artifacts consumed (Task 3.4 owns the sharedDocuments tier for the frontend); no runtime `gql` (graphql-tag UMD crash under the `@/backend/db` fixture chain's module conditions).

## 5. SEC audit (REQ-064 / REQ-072)

- **BFLA**: THE deliverable of this suite. 25/25 cells hard-asserted; 15 deny-cells (1 anon + 3 non-admin per admin surface ×4 admin surfaces… concretely: 5 anonymous + 10 non-admin FORBIDDEN + anonymous planCatalog/adminPlans) verified with the exact transport code; scope checks precede resolver bodies (mutation deny-cells carry syntactically valid variables that never execute). No cell skipped, no `.skip`, no conditional/soft assertion — grep-verifiable in the committed file.
- **BOPLA**: out of scope here (proven by 3.3's smuggle cells); this suite's inputs stay on the legal input surface.
- **Actor identity**: every request carries its own `Authorization: Bearer` per-request header (production client path) — no cookie/identity bleed between cells; four independently-registered public identities + one direct-DB admin identity.
- **Admin fixture**: engineered `users(role:"admin")` + `admin` child row with a REAL bcrypt hash + REAL public login — genuine session/token/scope check, mirroring the RegisterPublicRole BFLA exclusion (admin not publicly registrable).
- **Fixtures/PII**: randomized emails/phones/titles per suite run; rows accumulate by the established GraphQL integration-suite convention (no cleanup); no credentials in code beyond the non-literal-named shared test credential convention (`testCredential`, `sonarjs/no-hardcoded-passwords`-safe).

## 6. SR review (single-responsibility / structure)

- One suite = one concern: the wire-tier role matrix. Error-contract and BOPLA depth remain in 3.3's suite; REQ-060 shape depth shared via the `assertActiveOnlyCatalog` helper (used 5× — no dead helpers).
- The E2E round-trip chain (create → update → deactivate → reactivate) makes admin success cells depend on the captured `e2ePlanId` — an intentional, loudly-cascading chain (each dependent cell throws with an explicit message if upstream failed) that doubles as end-to-end lifecycle proof, sanctioned by the task instruction.
- Reuses sibling idioms verbatim (token fixtures, plan fixtures, document annotation style, fetchPolicy discipline) — zero logic duplication of production code; no production file, tasks.md, or sibling test file touched.

## 7. IV review (integration verification)

- Real wire, real DB, real auth: public `registerUser`/`login` for Student/Parent/Teacher; DB-engineered admin; direct `db.insert(plans)` fixtures for lifecycle control; everything exercised through the live schema on the warm server.
- Cross-suite interference check: sibling suites re-run AFTER this suite — `plan-catalog.queries.test.ts` + `plan-catalog.mutations.test.ts` = 24/24 green (7 + 17). No cache/token/fixture bleed (per-request bearer headers; no-cache reads on every post-mutation view).
- 401-vs-403 semantics, active-only filtering, admin full-catalog visibility, and lifecycle-driven visibility are each pinned at the same tier they will be consumed from.

## 8. Gate results

| Gate | Result |
|---|---|
| `bun run scripts/health/sub-loop.ts frontend/graphql/test/billing/plan-catalog.roles.test.ts --lifecycle duplicates` | ✅ exit 0 (tsgo / oxlint / biome / lint:type-aware / duplicates) — one `no-await-in-loop` finding on the visibility loop fixed in-file via `Promise.all` (four independent reads; no sequencing) |
| `bun run tsgo` (repo-wide) | ✅ 0 errors |
| Suite run | ✅ 27 pass / 0 fail / 830 expect() calls |
| Sibling re-run (cross-suite interference) | ✅ 24 pass / 0 fail (queries 7 + mutations 17) |
| Server health | ✅ warm :3000 server healthy throughout (`{ __typename: "Query" }` probe; no "Duplicate field" HMR poisoning — no restart needed) |
| Scope discipline | ✅ TEST-ONLY: no production file, no tasks.md, no sibling tests, no codegen/build, no dev-server restart |

## 9. Commit

- Commit: see worklog Task 3.6 entry (hash embedded there and in the git log; pushed to `origin/feat/dev1-005-plan-catalog-crud-admin-only` via `git push origin main:feat/dev1-005-plan-catalog-crud-admin-only`).
- Files: `frontend/graphql/test/billing/plan-catalog.roles.test.ts` (new), this outcome file. `worklog.md` deliberately NOT committed (per task rules).
