# Task 6.3 — Review Wave: Frontend (page, container, dialogs, navigation, documents)

Agent: review-frontend reviewer (Z.ai Code) · HEAD reviewed: `5f0c78e` (local `main` == `origin/feat/dev1-005-plan-catalog-crud-admin-only`)

## Verdict per check

| # | Check | Verdict | Evidence |
|---|---|---|---|
| 1 | `app/(dashboard)/admin/plans/page.tsx` guard + SSR hygiene | **PASS** | `withPageAuth({ roles: [UserRole.Admin], redirectTo: ADMIN_PLANS_ROUTE })` is the FIRST statement of the page body (L51); `UserRole` is a VALUE import from `@/backend/enum/users/user-role.enum` (L2); `getTranslations(locale)` is the single-argument SYNCHRONOUS idiom with `.plansTranslations` property access (L38, L57); ZERO GraphQL/document imports in the server file (imports are next / user-role enum / withPageAuth / views barrel / shared locale only) — also CI-pinned by `app/(dashboard)/admin/plans/test/admin-plans-page.guard.test.ts` "zero GraphQL imports" source-scan cell (8/8 green in the 4.2 record). |
| 2 | REQ-062 live guard (read-only wire spot-check) | **PASS** | `curl http://localhost:3000/admin/plans` (anonymous, no cookies) → in-band RSC redirect payload `NEXT_REDIRECT;replace;http://localhost:3000/login?redirect=%2Fadmin%2Fplans;307` thrown from `withPageAuth` (dev streaming SSR emits 200 + flight payload; the browser follows — matches 4.3 BF loop evidence). |
| 3 | sx-only styling (no inline `style` props) | **PASS** | grep `style={{` over `frontend/views/admin/plans/**` → 0 matches. All styling is `sx` (static objects or `theme =>` callbacks). |
| 4 | Theme-palette tokens only (no hex/rgb literals) | **PASS** | grep `#[0-9a-fA-F]{3,8}|rgb\(|rgba\(` over the plan's frontend views → 0 matches. Chips: `successContainer`/`onSuccessContainer` + mode-aware `grey[200|800|900]` via `inactiveChipPair(palette)`; borders/backgrounds via `outlineVariant`/`surfaceContainerLow`/`text.secondary`; shadows via `theme.palette.shadow.card`. |
| 5 | `*Outlined` icon naming | **PASS** | every `@mui/icons-material` import in the four view files aliases an `*Outlined` component: AddOutlined, Inventory2Outlined, SaveOutlined, PublishedWithChangesOutlined, CheckCircleOutlined, EditOutlined, UnpublishedOutlined. Zero non-Outlined imports. |
| 6 | `React.SubmitEvent` / event-type discipline | **PASS** | `PlanFormDialog.handleSubmit(event: React.SubmitEvent<HTMLFormElement>)` (L198); no bare `FormEvent` anywhere (grep → 0). `PlanStatusConfirmDialog` confirms via button `onClick`; container's `dismissSnackbar` uses the MUI signature `Event | React.SyntheticEvent`. Submit/confirm buttons are `loading` + `disabled` while pending (REQ-043, plus in-handler re-check). |
| 7 | Translation property-access ONLY (no hardcoded UI copy) | **PASS** | grep `t("`, `t('`, `t(\``, `labels[`, `translated[`, `errorsT[` → zero dynamic/string-key lookups in page + views. Full-file read: every table header, button, chip, dialog title/body/CTA, toast, empty/error state resolves through `PlansLabels` / `ErrorsLabels` property access. Only literals are CSS values, `data-testid`s, MUI enum props, and the locale-neutral em dash (punctuation, not copy). |
| 8 | aria-invalid on errored fields | **PASS** | `PlanFormDialog` drives MUI `TextField` `error` + `helperText` from `fieldErrors` (whitelisted five-field `extensions.fields[]` projection); MUI InputBase sets `aria-invalid` from the `error` prop — 4.4 browser cell (b) verified `aria-invalid=true` on the price field with the localized server message beneath. |
| 9 | `plan-catalog.documents.ts` naming + barrels + `id` | **PASS** | File at `frontend/graphql/sharedDocuments/billing/plan-catalog.documents.ts`; consts exactly `planCatalogQueryDocument` (`TypedDocumentNode<PlanCatalogQuery>` — no-arg form), `adminPlansQueryDocument` (+`AdminPlansQueryVariables`), `createPlanMutationDocument`, `updatePlanMutationDocument`, `setPlanActiveStatusMutationDocument` — names/types match the AGENTS.md operation-name table; imports from `"@apollo/client"` (never `/core`); all five selection sets carry the full ten-field `Plan` shape with `id` FIRST (REQ-061, test-enforced by `sharedDocuments/plan-catalog.documents.test.ts`); `billing/index.ts` re-exports the file; top-level `index.ts` re-exports `./billing`; consumers (container + both dialogs) import via the preferred barrel path; no `useLazyQuery` anywhere (grep over frontend+app → 0). |
| 10 | `navItems.ts` admin Plans entry | **PASS** | `{ route: "/admin/plans", labelKey: "plans", Icon: VerifiedOutlined }` in the Admin array (L77), positioned after teachers/students; Student/Teacher/Parent arrays contain no plans entry (structural admin-only visibility, no runtime filtering). No mobile bottom-nav exists anywhere in the codebase (grep `BottomNavigation` over frontend+app → 0), hence none was added. |
| 11 | i18n: 47-key parity + property-access type safety | **PASS** | `bun run test/scripts/run-test.ts shared/locale/plans-namespace.parity.test.ts` → **14 pass / 0 fail / 322 expects**; `EXPECTED_KEY_COUNT = 47` (the 4.4 `columnDeactivatedAt`/`columnCreatedAt` amendment); en+ar leaves both typed `PlansLabels` (compile-time parity gate); registry id `plans.plans`; `getTranslations(locale).plansTranslations` wiring pinned. |
| 12 | REQ-064 render matrix spot-check (deactivated rows + distinct chips) | **PASS** | Container issues `adminPlans({ includeInactive: true })` — the full catalog incl. deactivated rows; `PlanStatusChip` renders Active (`successContainer`/`onSuccessContainer` + CheckCircleOutlined) vs Inactive (mode-aware grey pair + UnpublishedOutlined) — visually distinct in both modes; row action flips label/icon per current state (`actionDeactivate` ↔ `actionActivate`). 4.3 BF evidence: deactivated Legacy Tajweed Plan 2025 renders موقوفة beside active rows; 4.4 BF: chip flip نشطة↔موقوفة with `deactivatedAt` populating/clearing. |
| 13 | Consumer surface = server-side filtering ONLY | **PASS** | grep `.filter(` over `frontend/views/**` → 0 matches (no client-side `isActive` filtering anywhere). Storefront `planCatalogQueryDocument` has no variables — filtering is server-owned; admin listing declares its intent via the `includeInactive` variable. |
| 14 | Component suites green | **PASS** | `bun run test:ui:components` → **72 pass / 0 fail / 333 expects across 9 files** (incl. PlanCatalogContainer 12-cell matrix, PlanFormDialog, PlanStatusConfirmDialog, dashboard-sidebar-nav suites). `bun run tsgo` → **0 errors** repo-wide. |

## Findings

### Resolved in this wave
None — zero in-scope defects found. No code changes were required; all gates re-run on the untouched tree (tsgo 0, test:ui:components 72/72, parity 14/14).

### Deferred (with owner)
1. **`sharedDocuments/AGENTS.md` Layout drift** (owner: **Task 6.1**, domain owner of `frontend/graphql/sharedDocuments/**`) — the Layout section's `billing/` line still reads "billing-alerts, billing-management, user-billing-profile, fx-ingestion", none of which exist in the tree; `plan-catalog.documents.ts` (this plan's REQ-061 artifact) is absent from the listing. Documented as a deviation by 4.1 already; strictly a doc-accuracy issue, no runtime impact (barrel discipline itself is correct and test-pinned). Fix = one-line AGENTS.md edit + step 5 of the AGENTS "new document file" checklist; blocked here by the "do NOT touch sharedDocuments/**" boundary.
2. **Pre-existing dev-mode hydration warning on `/admin/plans`** (owner: future Apollo-SSR task) — identical on `/profile` and `/student/dashboard`; server pass attempts the query, client re-renders the resolved tree. Final state correct; recorded by 4.3, reproduced posture unchanged at this HEAD. Not a plan defect.

## Gates executed (verification-only run, no files modified)
- `bun run tsgo` → 0 errors.
- `bun run test:ui:components` → 72 pass / 0 fail / 333 expects / 9 files.
- `bun run test/scripts/run-test.ts shared/locale/plans-namespace.parity.test.ts` → 14 pass / 0 fail / 322 expects.
- Read-only curl evidence against the warm :3000 dev server (guard wire behavior). No build; no restarts; backend/**, sharedDocuments/**, auth libs untouched.

## Scope of review (files inspected in full)
- `app/(dashboard)/admin/plans/page.tsx`
- `frontend/views/admin/plans/PlanCatalogContainer.tsx`, `PlanCatalogTable.tsx`, `PlanFormDialog.tsx`, `PlanStatusConfirmDialog.tsx`, `index.ts`
- `frontend/graphql/sharedDocuments/billing/plan-catalog.documents.ts`, `billing/index.ts`, top-level `index.ts`, `sharedDocuments/AGENTS.md`
- `frontend/views/dashboard/navItems.ts`
- `shared/locale/types/plans/index.ts`, `shared/locale/plans-namespace.parity.test.ts` (+ en/ar leaves via the parity suite)
