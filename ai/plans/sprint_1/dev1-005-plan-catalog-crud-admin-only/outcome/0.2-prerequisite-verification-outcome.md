# Task 0.2 — Prerequisite & Dependency Guard Verification — Outcome

**Task ID:** 0.2
**Agent:** prerequisite-verification subagent
**Date:** 2026-08-26 (verification session)
**Verdict:** **BLOCKED (scoped)** — Cells 0.2.1–0.2.4 and 0.2.IV verified PASS; **cell 0.2.5 FAILED** (DEV1-004 guarded-update precedent does not exist in the codebase). ❌ entry **D3** appended to `deferred-items.md` (D1/D2 rows untouched). Per 0.2.6, dependent tasks (2.2.3 `setActiveStatusOnce`, 2.3.4 `setPlanActiveStatus`) are BLOCKED until the Task 0.3 plan-review gate rules on the resolution path.

---

## 1. Per-Cell Evidence Table

| Cell | Dependency | Verdict | Evidence (file:line + symbol) |
|---|---|---|---|
| 0.2.1 | DEV1-001: `plans` table + 3 CHECKs; `paymentGateway`/`subscriptionStatus` enums; `PlanSelectType`/`PlanInsertType` | ✅ PASS | `backend/db/schema/billing/plans.ts:30` `check("plans_session_count_check", …)`, `:31` `check("plans_price_check", …)`, `:32` `check("plans_interval_days_check", …)` on `pgTable("plans", …)` (`:14`). `backend/db/schema/enums.ts:25` `export const paymentGateway = pgEnum("payment_gateway", […])`; `enums.ts:36` `export const subscriptionStatus = pgEnum("subscription_status", […])`. `backend/types/billing/plan.types.ts:3-4` `export type PlanSelectType = typeof plans.$inferSelect;` / `PlanInsertType = typeof plans.$inferInsert;`. **Live DB probe (postgres 17 @ 127.0.0.1:5432/app_db):** `SELECT conname FROM pg_constraint WHERE conrelid='plans'::regclass AND contype='c'` → exactly `plans_session_count_check`, `plans_price_check`, `plans_interval_days_check` (3 rows); `SELECT typname FROM pg_type WHERE typname IN ('payment_gateway','subscription_status')` → both rows present. |
| 0.2.2 | DEV1-002: `registerUser`/`createAdminUser` service paths; `isUniqueViolation` cause-chain precedent (REQ-052) | ✅ PASS | `backend/services/auth/registration.service.ts:157` `export async function registerUser(`; `:199` `export async function createAdminUser(` (same `RegistrationService` namespace). Cause-chain precedent for reuse: `registration.service.ts:103-118` `function isUniqueViolation(error)` — cycle-safe `DrizzleQueryError.cause` traversal with `seen` Set detecting PG `23505`; mirrored by `backend/lib/errors.ts:175-185` (`translateDbError`: `hasPgCode(error, "23505") → ConflictError`) and `backend/lib/errors/error-masking.ts:803` (cycle-guarded traversal, 23505/SQLite UNIQUE → localized CONFLICT). Reusable shape for REQ-052's `23514` → `ValidationError` translation. |
| 0.2.3 | DEV2-001: JWT context factory (`ctx.user`/`ctx.role`/`ctx.locale`); `withPageAuth({ roles, redirectTo })` | ✅ PASS | `backend/graphql/gqlContextFactory.ts:41-91` `Context` interface declares `locale` (`:43`), `user` (`:73`), `role` (`:81`); `createGraphQLContext` (`:154`) JWT-verifies via `verifyAccessToken(accessToken)` (`:191`) and returns `{ locale, …, user, …, role, … }` (`:213-225`). `shared/locale/` structure confirmed (imported at `gqlContextFactory.ts:31` `getServerTranslations` from `@/shared/locale/server-graphql`; dir listing shows en/ar namespaces + `errors` namespace). Page guard: `frontend/lib/auth/withPageAuth.ts:34-47` `WithPageAuthOptions { roles?: readonly UserRole[]; redirectTo?: string }`; implementation `:67` `export async function withPageAuth(options?)`; success contract `{ userId, user, role }` (`:50-57`, returned `:91-95`). |
| 0.2.4 | DEV2-002: `role` authScope, OR semantics, fail-closed; `authenticated` → `UnauthorizedError` (401) | ✅ PASS (path drift noted) | ⚠️ `backend/graphql/gqlSchemaBuilder.ts` **does not exist** (Glob `**/gqlSchemaBuilder*` → 0 hits). The `gqlSchemaBuilder` symbol is defined in **`backend/graphql/pothos/builder.ts:66`**: `export const gqlSchemaBuilder = new SchemaBuilder<{ … AuthScopes: { authenticated: boolean; role: UserRole[]; … } }>` (`:69-80`). Scope implementations (`builder.ts:108-128`): `authenticated: () => { if (!ctx.user) { throw new UnauthorizedError("Authentication required."); } return true; }` (`:113-118`) — 401 boundary (comment `:109-112`), `UnauthorizedError` → code `UNAUTHORIZED` (`backend/lib/errors.ts:44-48`); `role: (roles: UserRole[]) => (ctx.role ? roles.includes(ctx.role) : false)` (`:120`) — OR semantics, **fail-closed** (returns `false` when `ctx.role` is null; contract documented at `backend/graphql/query/teachers/applicant.query.ts:19`). Scope-failure mapping locked at source (`builder.ts:84-107`): explicit scope throws pass through verbatim; boolean-scope misses map to localized `ForbiddenError` (403, never conflated with 401). |
| 0.2.5 | DEV1-004 guarded-update precedent (`grantFreeTrialOnce`-pattern) as reference implementation for REQ-014/015 | ❌ **MISSING** | Repo-wide grep: `grantFreeTrialOnce` → **0 hits in `backend/`** (only in `ai/plans/sprint_0/dev1-004-free-trial-session-provisioning/{plan,tasks,specs}.md` and this plan's docs). `balanceTrial`/`trialGrantedAt`/`balance_trial`/`trial_granted_at` → **0 hits in `backend/`** (`backend/db/schema/students/students.ts:24-26` has only `balance_hifz`/`balance_reviews`/`balance_tajweed`). Guarded conditional UPDATE pattern `.where(and(eq(…), isNull(…)))` + `.returning()` → `isNull(` → **0 hits in `backend/db/repo`**; the only repo `.update(` sites are `backend/db/repo/teachers/applicant.repository.ts:145,149` (single-statement `UPDATE applicants … WHERE id … RETURNING` — **unguarded** predicate) and `backend/services/auth/auth.service.ts:241` (unguarded). `ai/plans/sprint_0/dev1-004-free-trial-session-provisioning/` has **no `outcome/` dir** → ticket planned, never executed; the precedent exists as a *documented plan* (sprint_0 `plan.md` D2 ruling `:69` + `grantFreeTrialOnce` sketch `:206`) but **not as code**. → ❌ **D3** recorded in `deferred-items.md`. |
| 0.2.6 | Conditional: ❌ entry if missing, else evidence | ✅ EXECUTED (missing-artifact branch) | ❌ **D3** appended to `deferred-items.md` (D1/D2 rows preserved verbatim); verdict BLOCKED (scoped) reported here and in worklog; dependent tasks 2.2.3/2.3.4 flagged as blocked pending the 0.3 gate ruling. Cells 0.2.1–0.2.4 evidence recorded above. |
| 0.2.SR | Semantic review — every cell has verifiable evidence | ✅ DONE | Every cell above cites file:line + symbol; DB claims are backed by a live psql probe (transcript in 0.2.1 row); absences (0.2.5, gqlSchemaBuilder.ts path) are backed by exhaustive Grep/Glob probes, not assumptions. |
| 0.2.IV | REQ-081 addendum authorization for the activation-flag schema delta (A-category) | ✅ CONFIRMED (with sequencing finding — see §2) | Authorization is explicit and on record in the ticket spec: `specs.md:6` (Critical reconciliation note, quoted in §2). The addendum itself is **not yet present** in `docs/specs/open-decisions-and-gaps.md` — by the plan's own design it is a knowledge-propagation artifact (REQ-081 trigger + Task 7.2.2). |
| 0.2.OD | Outcome file | ✅ DONE | This file. |

---

## 2. Instruction Verification (0.2.IV) — REQ-081 authorization quote

**Finding:** `docs/specs/open-decisions-and-gaps.md` (258 lines, read in full) currently contains **no REQ-081 addendum** — exhaustive Grep for `REQ-081|addendum|is_active|activation|schema delta` over that file returns **0 hits**. This is consistent with the plan's own sequencing, because REQ-081 is explicitly future-conditional on knowledge propagation, and the decisions-doc addendum is scheduled work:

> **REQ-081** (`ai/plans/sprint_1/dev1-005-plan-catalog-crud-admin-only/specs.md:152`): "WHEN knowledge propagation runs THEN `docs/specs/state-machine-invariants.md` SHALL gain a 'Plan Catalog Lifecycle' section with invariants **INV-PC1** … **INV-PC3** …; and `docs/specs/open-decisions-and-gaps.md` SHALL gain a resolved addendum recording: **the activation-flag schema delta (A-category addendum)**, forward-only edit semantics, title-encoded taxonomy (FR-2.2 reaffirmed), and the create double-submit tolerance ruling (REQ-043)."

> **Task 7.2.2** (`tasks.md:585`): "Add resolved addendum to `open-decisions-and-gaps.md`: activation-flag schema delta (A-category), reactivation semantics (marker cleared, audit history via DEV3-020), forward-only edit semantics, title-encoded taxonomy (FR-2.2 reaffirmed), verification-plan lookup rule ownership (FR-2.3 → DEV1-006/DEV2-005), create double-submit tolerance ruling (REQ-043), no-pagination/no-index rulings with revisit triggers"

**The authorization for the A-category schema delta is nonetheless explicit and on record TODAY in the ticket spec itself** (the normative requirement source for this ticket):

> `specs.md:6` — "The `plans` table delivered by DEV1-001 (`backend/db/schema/billing/plans.ts`) contains **no activation/status column**. The ticket's acceptance criteria require 'activate / deactivate' semantics. **This ticket therefore carries an explicitly authorized schema delta**: `plans.is_active boolean NOT NULL DEFAULT true` and `plans.deactivated_at timestamp NULL`, applied via `bun run db push` — following the DEV1-004 precedent for per-ticket schema deltas (`balance_trial`/`trial_granted_at`). The addendum is recorded in `docs/specs/open-decisions-and-gaps.md` (REQ-081). No other structural drift is permitted."

**Conclusion:** the activation-flag schema delta (A-category) is authorized — the delta's exact shape (`is_active boolean NOT NULL DEFAULT true`, `deactivated_at timestamp NULL`), its application mechanism (`bun run db push` only), and its boundary ("No other structural drift is permitted") are all quoted above from `specs.md:6` + REQ-081 (`specs.md:152`). **Caveat for the 0.3 gate:** `specs.md:6`'s clause "The addendum is recorded in `docs/specs/open-decisions-and-gaps.md`" is forward-looking — the decisions doc does not yet contain it (expected to land at Task 7.2.2). Recommended gate action: either pre-seed the REQ-081 addendum via the 0.3.2 pre-implementation amendment, or record an explicit confirmation checkpoint at 7.2.2, so the spec's present-tense claim is not treated as already satisfied.

---

## 3. Additional path-drift findings for the 0.3 gate (non-blocking)

1. **`gqlSchemaBuilder.ts` path drift (0.2.4):** REQ-004 (`specs.md:49`) and tasks.md 0.2.4 cite `backend/graphql/gqlSchemaBuilder.ts`; the artifact actually lives in `backend/graphql/pothos/builder.ts` (symbol `gqlSchemaBuilder`, `:66`). Artifact is present and contract-conformant; only the citation path is stale.
2. **DEV1-004 precedent path drift (0.2.5):** `plan.md:5` cites `ai/plans/dev1-004-free-trial-session-provisioning/plan.md`; the actual path is `ai/plans/sprint_0/dev1-004-free-trial-session-provisioning/plan.md`.

## 4. Implications for Phase 1 (and beyond)

- **Phase 1 is unblocked** with respect to schema work: Task 1.1's delta (`isActive`, `deactivatedAt` on `plans`, via `bun run db push` only) is authorized by `specs.md:6` (§2 above) and DEV1-001 ground truth is verified (0.2.1). The `students`/plans canonical-type surfaces (0.2.1) and registration/error-translation precedents (0.2.2) that Phase 1–3 reuse are in place.
- **Tasks 2.2.3 (`setActiveStatusOnce`) / 2.3.4 (`setPlanActiveStatus`) are BLOCKED** on D3: the plan's D2 ruling claims "Direct reuse of the DEV1-004 `grantFreeTrialOnce` atomicity pattern (proven, reviewed)" — no such reviewed implementation exists in the codebase. Resolution belongs to the **Task 0.3 plan-review gate** (which must pass before implementation anyway): either (a) amend D2 to make the REQ-014/015-specified guarded UPDATE (`specs.md:57-58`, SQL fully normative) the spec-defined pattern with sprint_0 DEV1-004's plan doc as documented reference only, or (b) sequence the DEV1-004 precedent implementation ahead of 2.2. Neither option requires inventing a parallel substrate — option (a) changes a provenance claim, not the design (the SQL, TOCTOU-window-zero argument, and `PLAN_ALREADY_*` mapping are unchanged and fully specified).
- **The 401/403 boundary and RBAC surface are as assumed** (0.2.3/0.2.4): GraphQL mutation fields for admin-only plan CRUD can rely on `authScopes: { role: [UserRole.Admin] }` + `authenticated`, and admin pages on `withPageAuth({ roles: [UserRole.Admin], redirectTo })`, with fail-closed semantics verified at symbol level.
- **REQ-052 translation precedent reusable:** the `isUniqueViolation`/`translateDbError` cycle-safe cause-chain traversal (0.2.2) is the exact shape REQ-052 requires for the new `23514` → `ValidationError` translation; expect a `plans`-targeted sibling in Task 2.x.

## 5. Verdict

**BLOCKED (scoped).** No code changes were made to `backend/` or `frontend/` (verification-only task). Artifacts touched: `deferred-items.md` (D3 appended), this outcome file, `tasks.md` checkboxes (0.2.5 left unchecked; all other 0.2 items checked), `worklog.md` (append). Next action: Task 0.3 plan-review gate must adjudicate D3 (and note §3 path drifts + §2 addendum-sequencing caveat) before implementation begins.
