/**
 * Billing domain services barrel.
 *
 * Per root `AGENTS.md` barrel conventions: relative `./` paths only,
 * `export *` re-exports, parent barrels never reach into nested files
 * directly.
 */
export * from "./plan-catalog.service";
